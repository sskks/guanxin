# 观心项目状态

> 最后更新：2026-06-24
> 当前阶段：v1.2 MVP 收口中

---

## 1. 当前判断

项目已经完成了 `可体验 MVP` 的主体框架，但还不能简单定义为“全部完成并稳定上线”。

从产品方向上看，`观心` 当前应明确定位为：

- 以情绪陪伴为核心的产品
- 以优秀传统文化为内容底座的启发式产品
- 帮助用户把“领悟”转化为现实生活实践的自我成长产品

更准确地说，当前状态是：

- 核心单人体验已经打通
- 第一阶段留存能力已经部分落地
- 文档体系已经建立
- 项目级 `PRODUCT.md` / `DESIGN.md` 已建立
- 前后端商业化演进骨架已建立
- AI 可用性、首次可理解性、导航一致性仍然是明显短板
- 测试覆盖和工程拆分仍然是明显短板

因此，当前更适合定义为：

**MVP 已可运行，正在进行第一阶段收口与内容表达升级。**

### 1.1 今日新增战略确认（2026-06-24）

本轮已确认项目的传统文化主链路，后续页面与功能都应围绕这一条主线收口：

**八卦为体，梅花为用，命宫 / 生日本命卦为人，五行为翻译层，节气为留存层。**

这意味着：

- 主底层回到八卦、六十四卦、《周易》象义与梅花易数式问事逻辑。
- 命宫与生日本命卦继续承担个体画像层。
- 五行继续作为翻译层，而不是单独做成一套新命理系统。
- 节气将承担后续的日更内容和回访理由。
- 八星八宅保留，但明确退到副层观察模块，不再继续作为主术数底层扩张。

---

## 2. 已完成

### 2.1 核心体验

- 启动页、生日录入、加载页、首页主路径已建立
- 命宫解读已接入
- 每日一卦可用
- 竹签摇卦可用
- 数字起卦可用
- 统一结果页可用
- 卦象图鉴可用
- 八星磁场页可用
- 历史记录页可用
- 好友互占分享链路已接入

### 2.2 第一阶段产品优化

- 主路径已调整为“先填生辰信息”
- 免责声明已前置到关键页面
- AI 结果支持结构化“行动卡”
- 历史记录支持主题筛选
- 首页已接入 7 天回看逻辑
- AI 错误提示已做友好化处理
- 收藏功能已补即时反馈

### 2.3 基础文档

- `docs/PRD.md`
- `docs/ARCHITECTURE.md`
- `docs/DEVELOPMENT.md`
- `docs/PROJECT_STATE.md`
- `docs/PRODUCT_STRATEGY.md`
- `docs/DEVELOPMENT_PLAN.md`
- `PRODUCT.md`
- `DESIGN.md`
- `docs/COMMERCIAL_ARCHITECTURE.md`
- `docs/UI_UX_UPGRADE_PLAN.md`

### 2.4 架构骨架

- `apps/web`
- `apps/api`
- `packages/design-system`
- `packages/domain`
- `packages/content`
- `.impeccable/live/config.json`

说明：以上目录目前是迁移骨架，当前真实运行入口仍是根目录 `index.html`。

---

## 3. 进行中

### 3.1 第一阶段收口

以下能力已经有代码或界面接入，但还不应算“完整验收通过”：

- `7 天回看`
  已有记录字段、首页卡片和回看逻辑，但缺少稳定的真实场景验收。

- `历史主题档案`
  已有关键词分类与主题条展示，但仍需结合真实记录验证筛选、收藏、搜索联动体验。

- `AI 双通路`
  当前支持浏览器直连 Qwen Key 与代理兜底两种方式，目标是先保证可用；后续还需要统一安全策略。

### 3.2 文档与代码一致性修正

- README 已升级，但部分表述偏“理想状态”
- PRD 和状态文档此前对完成度写得过满，正在校准为真实进度

### 3.3 最新用户测试反馈（2026-06-22）

朋友测试反馈已明确暴露出以下问题：

- 返回按钮位置不统一，且缺少顺手的返回上一页体验
- 零基础用户不理解周易术语，也不知道如何开始使用
- AI 回复偏长、偏散，不够直接
- 用户看完结果后不知道下一步该做什么

这意味着当前项目的第一阶段收口重点，需要从“功能是否存在”进一步转向：

- 是否好返回
- 是否看得懂
- 是否抓得住重点
- 是否知道下一步

### 3.4 本轮已开始实现（2026-06-22）

围绕用户反馈，当前已在 `index.html` 完成第一批 P0 收口改动：

- 新增内部导航栈，返回优先回到上一页，而不是默认回首页。
- 在占卦结果、每日一卦、摇签、历史、命宫解读、卦象图鉴等关键页面接入右滑返回上一页。
- 修正生日录入后的加载页和结果页跳转，避免加载页污染返回栈。
- 命宫解读动态页面中的返回按钮已从“返回首页”调整为“返回上一页”，无历史时兜底回首页。
- 新手引导从功能说明改为零基础白话引导，增加“卦、爻、命宫”的大白话解释。
- AI prompt 已从“大师长文解读”调整为“情绪产品里的传统文化解读助手”，要求短句、直接、可执行。
- AI 输出字段已调整为“一句结论、当前提醒、建议行动、今日避免、一句提醒、详细解读”，同时兼容旧字段。
- 结果页“先做这一件事”已支持根据默认总结或 AI 行动建议动态更新。

待验收：

- 需要在真实浏览器中验证右滑返回是否与页面滚动、每日一卦切换、弹窗交互冲突。
- 需要用真实 Qwen Key 或后端代理验证 AI 返回格式、长度和行动卡展示。
- 需要移动端 375px 宽度验证新手引导和结果页是否无横向溢出。

### 3.5 二次体验修正（2026-06-22）

基于最新测试反馈，第一批 P0 收口仍存在“变化不明显、解释不够深、分享弱、跳转不精准”的问题。本轮已继续修改：

- `命理档案页`
  - 新增左上角返回栏，不再只依赖页面底部入口。
  - 新增“先用白话看懂自己”卡片，解释命宫、本命卦、流年宫、五行生克分别和用户有什么关系。
  - 增加现实建议，不再只展示术语和长段传统描述。

- `占卦结果页`
  - 保留左上角返回，移除底部“返回上一页”按钮，避免返回入口出现在页面最底部。
  - 新增“先用白话看懂”卡片，用普通话解释当前卦不是判决，而是帮助用户分辨处境。
  - 新增动爻、互卦、变卦的大白话解释。
  - “先做这一件事”改为可执行动作，不再只是压缩卦辞或象辞。

- `卦象图鉴`
  - 结果页“看懂这个卦”改为直接打开当前卦详情，不再跳到图鉴列表让用户自己找。
  - 图鉴详情新增“白话先看”和“术语怎么理解”，降低零基础用户理解门槛。

- `分享能力`
  - 微信/系统分享文案从一行字改为结构化内容：卦名、问题、白话启发、今天先做的一件事、互卦/变卦延伸观察。
  - 分享卡片 UI 重做，突出“白话启发”和“今天先做这一件事”，弱化旧式卦辞堆叠。

- `AI 解读`
  - prompt 进一步要求“建议行动”必须是今天能完成的小动作，禁止只复述卦辞或使用“顺势、谨慎、等待”等空泛词。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
- 关键功能字符串与函数接入检查通过。

待验收：

- 需要在浏览器中逐页点击验证：命理档案页、占卦结果页、卦象图鉴详情、分享卡片预览。
- 需要移动端宽度验证分享卡片和新卡片是否视觉稳定。

### 3.6 第三次体验修正（2026-06-22）

基于最新反馈，继续修正以下问题：

- `占卦限制`
  - 已取消“今日已占 / 一日一占”的前端限制。
  - 同一天可以多次问卦、数字起卦，并分别保存为独立历史记录。
  - IndexedDB 记录策略调整为唯一记录 key，避免同一天记录互相覆盖；保留旧记录兼容读取。

- `本命卦解读`
  - 命理档案页新增“本命卦详细解读”。
  - 解读基于真实计算出的本命卦 `id / name / 上下卦 / 卦辞 / 象辞 / 关键词` 生成，不再泛泛只讲命宫。
  - 增加“它从哪里来、它在提醒什么、传统文本、怎么用到生活里”四层说明。
  - 命宫解读页同步增加本命卦详细解读。

- `卦象图鉴入口`
  - 结果页按钮从“看懂这个卦”调整为“深入学习此卦”。
  - 图鉴详情移除与结果页重复的“白话先看”，改为承接延展学习：卦辞、传统解读、上下卦结构、术语说明、关键词。

已验证：

- `index.html` 内联脚本语法检查通过。
- 关键功能接入检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
- 浏览器实际验证：同一天连续完成两次数字起卦后，历史页同时展示两条独立记录，没有互相覆盖。

待验收：

- 命理档案页和命宫解读页检查本命卦详细解读是否展示当前用户真实本命卦。
- 从占卦结果页点击“深入学习此卦”，确认图鉴详情不再与结果页白话卡重复。

### 3.7 占卦历史覆盖专项修复（2026-06-22）

本轮针对“占卦历史会覆盖”的反馈做了专项收口：

- `IndexedDB 记录模型`
  - 新增精确主键读取函数，收藏、七日回看、AI 结果回写不再通过日期索引兜底查找。
  - 新记录继续使用 `mei_...` / `num_...` 唯一 key 作为 `date` 主键，`day` 只作为日期分组字段。
  - 旧记录仍保留兼容读取，不强制迁移，避免破坏用户已有历史。

- `AI 解读回写`
  - AI 解读只在存在当前记录唯一 key 时写回。
  - 好友分享、无本地记录或旧自然日期参数不会再回退到“当天日期”覆盖历史。

- `收藏与历史`
  - 历史列表的收藏操作只按当前卡片的唯一记录 key 更新，不再按 `day` 索引取同日第一条。
  - 已验证同一天连续两次数字起卦会进入历史页并同时显示。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
- 浏览器实际页面验证：历史页渲染出两条同日测试记录，记录问题文本分别保留。

遗留说明：

- 当前产品还没有“删除单条历史”功能，本轮浏览器验证产生的本地测试记录无法通过 UI 清理；后续可在历史功能迭代中补删除/清空能力。

### 3.8 摇签翻转卡片位置修复（2026-06-22）

本轮针对“摇签后的卡片 UI 翻转之后位置不一致”的反馈做了专项修复：

- `摇签翻转卡片`
  - 将翻转弹层改为垂直居中布局，卡片与提示文字使用统一布局流，不再让提示文字绝对定位在底部。
  - 固定 `.flip-card-container` 的中心、宽高与透视容器，避免翻转前后视觉中心偏移。
  - 统一正反面的 `width / height / box-sizing / backface-visibility`，减少移动端浏览器 3D 翻转抖动。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
- 浏览器实际页面验证：数字起卦触发翻转卡片后，自动翻转前后卡片外框 `left / top / width / height` 保持一致。

### 3.9 提交前状态同步（2026-06-23）

本次准备提交到 GitHub 的内容包含：

- `主体验修复`
  - 生辰信息作为主路径入口。
  - 免责声明与使用边界前置。
  - 左上角返回与右滑返回体验补齐。
  - 结果页、命宫解读、图鉴详情增加更白话、更行动导向的说明。
  - 摇签翻转卡片位置修复。

- `数据与留存`
  - 占卦历史改为唯一记录 key，解决同一天记录覆盖问题。
  - 收藏、AI 回写、七日回看改为按精确记录读取。
  - 保留旧记录兼容读取。

- `AI 与后端代理`
  - 本地 `server.py` 保留 AI 代理能力。
  - AI prompt 收敛为更短、更直接、更行动导向。
  - 仍需线上环境继续验证 DashScope Key 与代理配置。

- `架构与文档`
  - 新增产品定位、设计系统、商业化架构、开发计划等文档。
  - 建立 `apps/` 与 `packages/` 迁移骨架，但当前真实运行入口仍是 `index.html`。
  - `.impeccable/` 已加入 `.gitignore`，避免提交本地设计工具状态。

提交前验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
- 未检出真实 API Key。

线上测试重点：

- 首次进入是否能顺畅完成生辰信息填写。
- AI 解读是否能在部署环境跑通。
- 同一天连续占卦是否都进入历史。
- 收藏当前结果是否有即时提示与状态变化。
- 摇签翻转卡片是否保持居中稳定。

### 3.10 GitHub Pages 部署配置（2026-06-23）

本轮新增 GitHub Pages 自动部署配置：

- `.github/workflows/pages.yml`
  - 监听 `main` 分支 push。
  - 使用 GitHub 官方 `actions/configure-pages`、`actions/upload-pages-artifact`、`actions/deploy-pages`。
  - 直接部署仓库根目录，当前入口仍为 `index.html`。

- `.nojekyll`
  - 避免 GitHub Pages 使用 Jekyll 处理静态文件。
  - 保持纯静态 SPA 文件按原样发布。

部署说明：

- 当前环境无法写入 `.git/index.lock`，所以不能由 Codex 直接完成 commit / push。
- 用户在本机执行提交并推送到 `origin/main` 后，GitHub Actions 会自动触发 Pages 部署。
- 如果仓库尚未开启 Pages，需要在 GitHub 仓库设置中将 Pages 来源选择为 `GitHub Actions`。

### 3.11 线上文件与体验反馈修正（2026-06-23）

基于线上测试反馈，本轮确认并修正以下问题：

- `线上不是最新`
  - 本地 `index.html` 是唯一运行入口；此前重复存在的 `prototype.html` 容易造成“是否部署错文件”的判断混乱。
  - GitHub Pages 工作流已改为只发布 `_site/index.html` 和 `.nojekyll`，不再把历史补丁脚本等旧文件放进部署产物。
  - `index.html` 新增版本标识 `guanxin-version=2026-06-23-ui-sync`，后续线上可通过页面源代码确认是否加载最新版本。

- `摇签翻转卡片`
  - 翻转卡片改为固定在视口中心，围绕自身中心轴原地翻转。
  - 提示文字改为绝对定位在卡片下方，不参与卡片布局，避免翻转时上下漂移。

- `本命卦解读`
  - 删除大段笼统长文式表达。
  - 改为“核心提醒 + 优势 + 容易卡住 + 今天先做 + 回看问题”的短卡片结构。
  - 目标是让零基础用户先抓住重点，再决定是否继续看传统文本。

### 3.12 命理档案页信息密度收口（2026-06-23）

基于最新反馈，本轮继续收口命理档案页与命宫解读页：

- `命理档案页`
  - 不再常驻展示命宫、本命卦、流年宫的大段解释。
  - 主页面改为“先抓重点，再按需展开”，只保留概要提示与点击入口。
  - 本命卦、命宫、流年宫都改为可点击项，进入统一解释弹层查看。

- `命宫解读页`
  - 删除页面内连续堆叠的大段命宫、本命卦、流年说明。
  - 保留摘要和关系标签，详细解释改为点按展开。
  - 目标是让用户先理解“该点哪里”，而不是一进页面就被长内容压住。

- `文件结构`
  - 运行入口已收敛为单一 `index.html`，避免后续再次出现双文件内容分叉。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
- 浏览器实际页面验证：命理档案页已变为“概要 + 点击展开解释”；点击本命卦入口后可弹出解释面板。

### 3.13 命宫弹层与翻签体验细修（2026-06-23）

基于最新截图反馈，本轮继续修正命宫弹层与摇签翻面体验：

- `命宫 / 本命卦解释弹层`
  - 解释弹层的滚动区域从整张面板改为仅正文区域滚动，标题和关闭按钮固定在顶部，不会再因为上下滑动而丢失关闭入口。
  - 命宫弹层不再重复渲染四个同标题“关键词”卡片，改为“你的底色 / 你更容易怎么反应 / 能学到什么 / 今年怎么用”四种不同信息层次。
  - 命宫内容补充“你容易卡在哪 / 教学理解 / 怎么落到今天”，不再只做字面转述，而是强调默认反应、成长课题和当下行动。

- `摇签翻转卡片`
  - 翻签背面重新收口为单张签卡的正反面视觉，不再像一根签筒翻成一张新卡。
  - 调整翻转容器尺寸、背面装饰和提示文案，让动画更接近“原地翻面”而不是上下位移切换。
  - 放宽正面空白区域点击翻回背面的条件，同时保留正文滚动区和按钮点击不误触翻面。

- `命宫解读页`
  - 清理已不再参与页面渲染的旧五行色值、旧长文变量，避免继续堆积历史逻辑。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 200，且首页源代码包含 `guanxin-version=2026-06-23-ui-sync`。
- 项目已收敛为单入口 `index.html`，不再保留同步副本文件。

### 3.14 命宫解读结果模式重设计（2026-06-23）

基于“当前解读不够精准、缺少吸引力、用户不容易被带入”的反馈，本轮对命宫解读页做了结果模式级重设计：

- `命宫解读页结构`
  - 从“摘要 + 术语入口列表”改为三层结果结构：
    - 第一层：命宫主结论 Hero，直接告诉用户“你最容易怎样反应、为什么会这样”。
    - 第二层：现实场景卡片，把命宫落到工作推进、关系相处、情绪低谷三个用户最关心的场景。
    - 第三层：按需展开的深读入口，再进入命宫、本命卦、流年和术语解释。

- `体验目标`
  - 先让用户快速感到“像在说我”。
  - 再让用户知道“为什么会这样”。
  - 最后才把传统文化学习内容作为延展入口，而不是一开始就压给用户。

- `UI 变化`
  - 新增命宫 Hero 卡、三张速览卡、现实场景卡组、深读入口区。
  - 信息层级从“平铺文本”改为“结论优先、场景承接、学习后置”。

已验证：

- `index.html` 内联脚本语法检查通过。

### 3.15 命宫解读分段式体验升级（2026-06-23）

基于“参考优秀产品模式再作优化创新”的要求，本轮继续把命宫解读页从单次长阅读，升级为更像成熟内容产品的分段消费体验：

- `交互模式`
  - 新增三段式顶部切换：
    - `先看结论`
    - `看场景`
    - `去深读`
  - 用户不必一口气滚完整页，而是按当前需求切换消费内容。

- `产品化方向`
  - 借鉴成熟内容/陪伴类产品“重点先行、分层展开、学习后置”的思路，但保留观心自己的传统文化语境。
  - 首页不是先讲术语，而是先建立“像在说我”的吸引力，再把兴趣引导到深读内容。

- `UI 变化`
  - 新增粘性分段切换条。
  - 命宫页内容被拆为三块独立 panel，降低首屏压迫感，增强控制感和节奏感。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 200，版本标记可读。

### 3.16 命宫深读弹层统一节奏（2026-06-23）

基于“不要一上来堆长文本，但要让愿意学习的用户能深读”的要求，本轮把命宫、本命卦、流年三个解释入口统一为轻量深读弹层：

- `交互模式`
  - 页面主层只保留引导、结论和场景，避免结果页继续变长。
  - 用户点击“命宫深读 / 本命卦深读 / 流年深读”后，再进入弹层查看详细解释。
  - 弹层关闭按钮固定在顶部，正文区域单独滚动，避免上下滑动后找不到关闭入口。

- `内容节奏`
  - 新增 step-card 结构，按“先给一句判断 -> 常见误区 -> 今天怎么做 -> 传统文化怎么理解”的节奏组织内容。
  - 命宫强调用户的默认情绪反应和修正方向。
  - 本命卦强调性格底层模式和可实践的成长方式。
  - 流年强调今年更适合练习的生活课题。

- `产品意义`
  - 主页面降低阅读压力，先满足普通用户“快速看懂我”的需求。
  - 深读弹层承接高兴趣用户，让他们在使用过程中逐步学习传统文化，而不是被术语一次性压住。

已验证：

- `index.html` 内联脚本语法检查通过。
- 页面包含 `renderInsightSteps()`、`openInsightSheet()`、`buildProfileInsightData()` 等深读弹层相关实现。

### 3.17 生辰主路径落点修正（2026-06-23）

浏览器实测发现：虽然新版命宫解读页已经实现，但用户从“填写生辰信息 -> 确认并计算”进入的首个结果仍是旧“命理档案”页，导致实际体验与产品方向不一致。

- `主路径调整`
  - 生辰信息提交后，加载完成直接进入新版“命宫解读”页。
  - 旧“命理档案”结构暂时保留，不在本轮大拆，避免影响已存在的解释弹层和历史逻辑。
  - 清理提交后的 `guanxin_pending_nav`，避免旧 pending 状态影响本次落点。

- `产品意义`
  - 用户第一次完成生辰填写后，直接看到“先看结论 / 看场景 / 去深读”的分段式体验。
  - 避免用户第一眼仍进入旧卡片堆叠结构，降低“改了但感觉变化不大”的问题。

已验证：

- 浏览器实测确认：重新提交一组生辰信息后，已直接进入新版“命宫解读”页。
- 浏览器实测确认：“先看结论 / 看场景 / 去深读”三段切换可用。

### 3.18 深读弹层默认短读化（2026-06-23）

浏览器实测发现：深读弹层虽然能打开，但旧的传统长文仍默认跟在 step-card 后方展示，打开后阅读压力仍然偏大。

- `弹层阅读节奏`
  - 默认只展示摘要、关键词和四步深读卡片。
  - 传统文本与补充解释改为折叠区，用户需要时再主动展开。
  - 保留传统文化学习入口，但不再干扰普通用户快速抓重点。

- `产品意义`
  - 更贴近“先解决情绪和行动，再进入文化学习”的产品定位。
  - 降低新用户首次阅读负担，同时给高兴趣用户保留继续学习的深度。

已验证：

- 浏览器实测确认：重新打开深读弹层后，传统长文默认折叠。
- 浏览器实测确认：弹层首句已改为白话判断，四步深读卡片正常显示。
- 浏览器实测确认：折叠区默认关闭，旧长文不会直接压到用户面前。

### 3.19 命宫今日练习闭环（2026-06-23）

基于 `product-manager-toolkit`、`product-generation-ops`、`ui-ux-pro-max`、`frontend-design`、`impeccable` 的组合方法，本轮不继续堆解释文本，而是把命宫解读页向“情绪安顿 -> 今日行动 -> 后续回看”推进。

- `产品判断`
  - 用户看懂命宫后，下一步不是继续读更多术语，而是先把启发转成一个现实动作。
  - 当前 MVP 最需要补的是行动闭环，而不是扩大功能数量。

- `页面更新`
  - 命宫解读页新增“今天先这样用”面板。
  - 面板包含“今天先做 / 今天先不要 / 晚上回看”三步。
  - 不同命宫会得到不同的行动、避免事项和回看问题。

- `留存机制`
  - 新增“记下今日练习”，暂存在本地 `localStorage`。
  - 新增“带着这个问题去问易”，会把命宫相关问题带入问卦弹窗。
  - 这是轻量留存雏形，后续可升级为 7 天回看、账号同步或后端记录。

- `体验原则`
  - 先白话，再术语。
  - 先行动，再长文。
  - 先本地闭环，再扩大社交或后端能力。

已验证：

- 浏览器实测确认：“今天先这样用”面板在生辰提交后的命宫页可见。
- 浏览器实测确认：“记下今日练习”点击后出现成功反馈；受浏览器只读验证环境限制，本轮无法直接读取页面存储，但页面事件未报错且反馈正常。
- 浏览器实测确认：“带着这个问题去问易”能回到首页、打开问卦弹窗，并自动预填命宫相关问题。

### 3.20 命宫弹层滚动与返回卡死修复（2026-06-23）

基于截图反馈，本轮修复两个影响主流程可用性的交互问题：

- `深读弹层展开后无法滚动`
  - 将解释弹层面板改为明确高度，正文区统一负责滚动。
  - 取消展开区内部嵌套滚动，避免移动端/内置浏览器滚动事件抢不到目标。
  - 修复 flex 子项被压缩导致内容被截断但不产生滚动高度的问题。

- `返回后卡在推演命理`
  - 生辰提交后的命宫页使用替换式进入，不再把 loading 页留作返回目标。
  - `goBack()` 增加 loading 跳过逻辑。
  - 命宫页顶部和底部返回改为直接回首页，避免回到启动页或 loading 过渡页。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 200，版本标记可读。
- 浏览器实测确认：命宫页顶部返回直接回首页，不再进入 loading。
- 浏览器实测确认：术语弹层展开后正文可真实滚动，关闭按钮保持可见。

---

### 3.21 命宫八宅八星文化层优化（2026-06-23）

基于“命宫解读已经变好，但仍希望减少重复表述，并引入准确的八星/八宅传统文化知识”的反馈，本轮完成一个轻量文化深度切片：

- `命宫解读页`
  - 主文案减少“默认反应 / 惯性”类重复表达，改为“情绪按钮、长期主题、今年节奏”的三层白话理解。
  - 新增“八宅八星自我镜”模块，使用已有 `calcBaZhai()` 计算结果，把八星分成“四个助力 / 四个提醒”两组。
  - 每颗星只在主页面显示星名、方位和一句生活场景，避免页面再次变长；点击后进入统一深读弹层。
- `八宅八星内容模型`
  - 新增八星白话解释：生气、天医、延年、伏位、绝命、五鬼、六煞、祸害。
  - 每颗星拆成“传统含义 / 现实场景 / 今天怎么用 / 边界提醒”四层，避免直接做吉凶恐吓或确定性断言。
  - 保留传统八宅中“东四命 / 西四命、四吉星 / 四凶星、八方与八卦五行对应”的框架，但产品表达统一转为情绪整理和自我观察。
- `术语准确性`
  - 页面新增术语边界说明：八宅里的“命卦/命宫”主要是个人方位系统；观心当前的“生日本命卦”是用生辰组合出的六十四卦观察层，两者相关但不是同一个概念。
  - 后续如果继续深化传统文化内容，应优先补“资料来源说明”和“术语校对表”，避免产品文案越写越像玄学断语。

待验收：

- 需要在浏览器中进入命宫解读页，确认“八宅八星自我镜”渲染正常。
- 需要点击至少一颗助力星和一颗提醒星，确认深读弹层可打开、可滚动、可关闭。
- 需要移动端宽度验证星卡不拥挤，文本不撑破布局。

---

### 3.22 传统文化术数体系梳理（2026-06-23）

基于“先梳理当前项目用到的术数，再优化精炼、补充不足”的要求，本轮先做知识底座和产品边界收口，避免继续盲目堆功能：

- `新增文档`
  - 新增 `docs/TRADITIONAL_CULTURE_SYSTEM.md`，梳理当前已使用的《周易》六十四卦、八卦、梅花易数轻量起卦、命宫/命卦/生日本命卦、八宅八星、五行生克、节气等体系。
  - 明确产品方向：不是算命工具，而是以《周易》象数为入口，以情绪整理、生活实践和传统文化学习为落点的东方自我观察产品。
- `术语边界`
  - 明确当前代码不是完整四柱八字系统，文档和页面中的“八字 / 排四柱”属于过度承诺，需要修正为“生辰信息 / 命宫 / 生日本命卦 / 流年宫”。
  - 明确“生日本命卦”是观心用生辰信息组合出的六十四卦观察层，不等同于八宅里的命卦。
  - 将八宅八星中的强吉凶词产品化为“助力 / 提醒”，降低恐吓式表达。
- `内容方法`
  - 后续所有术数解释统一使用“五层模板”：一句白话、对应你、传统根据、今天怎么用、不要误解。
  - 推荐补充“变易、简易、不易”“君子以修身”“观象而非迷信结果”三层传统文化哲思，支撑产品的情绪价值和学习价值。
- `下一步落地`
  - P0：修正文档和页面中“八字 / 排四柱”的表述，建立术语说明和来源标注。
  - P1：命宫页继续压缩重复文案，占卦结果页补体用关系，历史记录按主题聚合。
  - P2：每日一卦升级为“每日一象”，节气专题做成 24 个轻量内容节点，图鉴增加人生场景索引。

待验收：

- 检查 `docs/TRADITIONAL_CULTURE_SYSTEM.md` 是否覆盖当前项目实际使用的术数体系。
- 后续代码修改时优先修正可见页面的“八字 / 排四柱 / 本命卦”口径。

---

### 3.23 术语准确性首轮页面收口（2026-06-23）

基于 3.22 的术数体系梳理，本轮对可见页面做第一轮准确性修正：

- `生辰填写与加载流程`
  - 将“排四柱”改为“读生辰”，将“配纳甲”改为“定节律”，避免让用户误以为当前已经实现完整四柱八字排盘。
  - 将生辰说明中的“本命卦”改为“生日本命卦”，与八宅命卦做区分。
- `命理档案页`
  - 将“八字”标签改为“生辰节律”。
  - 将“本命卦”入口改为“生日本命卦”。
- `新手引导`
  - 将“命宫和本命卦档案”改为“命宫和生日本命卦档案”。

待验收：

- 需要在浏览器中重新进入生辰填写流程，确认加载步骤和档案摘要不再出现“八字 / 排四柱”误导性文案。

---

### 3.24 功能方向确认：四维生活解析（2026-06-23）

基于“围绕现代人关心的事业、感情、身体、钱财分维度解析，并评估紫微斗数或八字十神是否加入”的要求，本轮完成产品方向取舍：

- `确认主方向`
  - 下一阶段优先做“观心四维解析”：事业、感情、身心、钱财。
  - 四维解析不是新增独立命理系统，而是把现有命宫、生日本命卦、流年宫、八宅八星、五行关系重新组织成现代用户能理解的问题维度。
- `暂不加入的能力`
  - 暂不做紫微斗数正式排盘。原因是紫微需要农历年月日时、十二宫、十四主星、辅曜、四化、闰月、晚子时等完整排盘边界，当前单文件 MVP 不适合直接承诺准确性。
  - 暂不做八字十神正式解析。原因是十神必须依赖完整四柱、日主、五行旺衰、格局和大运流年；当前已经去掉四柱八字承诺，不能又加入十神断语。
- `可借鉴结构`
  - 可以借鉴紫微斗数“按宫位拆分人生问题”的产品结构，但页面命名为“四维生活解析”，不使用“紫微命盘”“十神排盘”等会误导用户的术语。
  - 每一维固定输出：一句白话判断、一个常见卡点、一个今日练习、一个传统根据。
- `风险边界`
  - 身心维度只做情绪和生活节律提醒，不做医疗判断。
  - 钱财维度只做资源管理和消费节奏提醒，不做投资建议。

已同步：

- `docs/TRADITIONAL_CULTURE_SYSTEM.md` 更新为 v0.3，新增本轮方向确认和紫微/八字十神边界。
- `docs/PRD.md` 更新为 v1.2，新增“四维生活解析”功能，并将紫微斗数正式排盘、八字十神正式排盘列入当前阶段非目标。

---

### 3.25 四维解析敏感表达边界确认（2026-06-23）

基于“四维解析会触及事业、感情、身心、钱财等敏感主题，不能制造恐慌，也不能掩盖不说”的反馈，本轮进一步收口内容安全标准：

- `总原则`
  - 四维解析必须客观但不冷漠：可以指出卡点和风险，但不能用“注定、必然、大凶、必破、必分”等断言。
  - 不能恐吓，也不能粉饰。表达应落在“这里需要留意什么、现实里先核对什么、今天能先做什么”。
  - 术数只作为观察镜子，不替代医疗、财务、法律、亲密关系等现实判断。
- `维度边界`
  - 事业：讲行动节奏、压力、协作和边界；不预测升职、裁员、考试、创业成败。
  - 感情：讲表达、边界、期待落差和修复；不判断对方爱不爱、是否出轨、是否复合、是否正缘。
  - 身心：讲压力负荷、睡眠节律、情绪消耗和自我照料；不诊断疾病、不解释病因、不替代医生或心理咨询。
  - 钱财：讲消费节奏、资源管理、安全感和风险意识；不预测发财破财，不推荐金融产品，不做投资建议。
- `固定输出结构`
  - 每一维必须包含：一句客观观察、可能卡点、现实核对、今日练习、传统根据、边界提醒。
  - 身心维度如果涉及持续痛苦、睡眠饮食明显受影响、无法完成日常任务等情况，应提示寻求专业帮助。
  - 钱财维度如果涉及投资、借贷、合同、担保、保险等，应提示独立核实风险并咨询合格专业人士。

已同步：

- `docs/TRADITIONAL_CULTURE_SYSTEM.md` 更新为 v0.4，新增四维解析表达边界、各维度可说/不可说、内容模板和资料来源。
- `docs/PRD.md` 补充四维解析验收标准和敏感维度风险应对。

---

### 3.26 术数表达三层法确认（2026-06-23）

基于“文字输出需要在温和修饰与术数本意之间取得平衡”的反馈，本轮确认后续内容表达方法：

- `核心方法`
  - 采用“术数骨架 -> 现实翻译 -> 情绪承接”的三层表达。
  - 先保留卦象、爻位、五行、八星、命宫等传统依据，再翻译成现实处境，最后给出可行动的承接。
- `避免两种偏差`
  - 不能过度修饰成普通心理鸡汤，否则会失去传统文化术数的魅力和准确感。
  - 不能直接使用恐吓式断语，否则会放大焦虑，削弱产品的情绪安顿价值。
- `验收标准`
  - 每段解读至少保留一个明确传统依据。
  - 每段解读必须指出一个真实卡点或现实核对点。
  - 每段解读必须给一个低风险、当天可做的小行动。
  - 敏感主题必须保留边界提醒。

已同步：

- `docs/TRADITIONAL_CULTURE_SYSTEM.md` 更新为 v0.5，新增“术数表达三层法”“文案强度控制”“从断语到观心表达示例”和内容验收标准。
- `docs/PRD.md` 新增产品原则：“先保留术数骨架，再做情绪承接”。

---

### 3.27 四维生活解析第一版接入命宫页（2026-06-23）
基于刚确认的产品方向，本轮开始把“四维生活解析”真正接到可见页面，而不是只停留在文档层：

- `命宫解读页`
  - 在“看场景”流中新增“事业 / 感情 / 身心 / 钱财”四维生活解析入口。
  - 每个维度先展示一句判断和一个“今天先做”的动作，避免用户一上来就读长文本。
  - 点击任一维度后，进入统一深读弹层，结构固定为：一句观察、容易卡住、现实核对、今天行动、传统依据、使用边界。
- `内容组织`
  - 四维内容当前完全基于现有命宫、生日本命卦、流年宫、五行关系组合生成，没有引入紫微斗数或八字十神等新排盘系统。
  - 页面内把“本命卦”可见标签继续统一向“生日本命卦”收拢，减少术语混淆。
- `当前价值`
  - 用户在命宫页不再只能看抽象命理摘要，而是能先从现实关心的话题切入。
  - 这也是“术数骨架 -> 现实翻译 -> 情绪承接”第一次在可见主页面上的正式落地。

已验证：

- `index.html` 内联脚本语法检查通过。

待验收：

- 需要在浏览器中确认四维卡片是否正常渲染、点击后弹层是否可打开。
- 需要验证移动端宽度下卡片是否保持单列且无横向溢出。
- 需要进一步评估四维卡片文案是否还存在重复表达，可在下一轮继续压缩。

补充更新（同日续做）：

- 已进一步压缩四维卡片动作文案，去掉“今天先做：今天先……”这类重复句式。
- 已修正新手引导遮罩显示边界：仅允许在首页场景激活，离开首页后自动收起，不再挡住命宫页等深层页面交互。
- 已再次用 390px 移动宽度验证：
  - 命宫页进入后引导遮罩不会拦截点击；
  - “看场景”流中的四维卡片仍可打开对应深读弹层。

## 4. 待开发

这些方向在文档中有规划，但当前尚未开始或未进入实现阶段：

### 4.0 下一批立即实现

这一批不是继续扩功能数量，而是把刚确认的理念真正落到页面和内容层：

- `四维生活解析第一版`
  - 在命宫解读页新增事业、感情、身心、钱财四个维度入口或卡片。
  - 每一维按固定结构输出：一句判断、一个卡点、一个现实核对、一个今日练习、一个传统根据、一个边界提醒。
- `术数表达三层法落地`
  - 命宫、本命卦、流年宫、八宅八星的现有解释，逐步改写为“术数骨架 -> 现实翻译 -> 情绪承接”。
  - 先处理命宫解读页和问卦结果页这两个最强感知页面。
- `AI 解读文案重构`
  - 让 AI 输出更贴近新的表达标准：既保留术数依据，又避免空泛和恐吓。
  - 对四维主题增加专门约束，不再只输出通用建议。
- `术语与边界统一`
  - 页面内继续清理“本命卦 / 生日本命卦”“八字 / 生辰节律”等混用。
  - 敏感主题统一增加边界提醒。

### 4.1 第二阶段：内容深度

- 今日一问
- 多层解读模式
- 人生场景图鉴增强
- 节气专题
- 月度回顾升级

### 4.2 第三阶段：轻社交验证

- 分享卡片 2.0
- 好友互占增强
- 一句回应
- 匿名主题观察

### 4.3 工程化补齐

- 拆分单文件 `index.html`
- 提取卦象数据为独立数据文件
- 为纯计算逻辑补自动化测试
- 梳理 IndexedDB 数据结构与迁移策略
- 将 `:root` 设计 token 抽入 `packages/design-system/tokens.css`
- 将 AI prompt 和术语解释迁入 `packages/content`
- 将起卦、命宫、八宅等纯逻辑迁入 `packages/domain`

---

## 5. 已知问题

### 高优先级

- AI 能力依赖用户本地 Key 或代理配置，稳定性仍不足
- 返回入口不统一，部分页面更像“回首页”而不是“回上一页”
- 新手理解门槛偏高，术语解释与场景引导不足
- AI 结果虽然已有结构化卡片，但仍存在冗长、不够动作导向的问题
- 结果页缺少明确下一步建议，闭环仍不够强
- 当前运行入口仍是超大单文件 `index.html`，虽然仓库已有 `css/`、`js/`、`data/` 目录，但拆分尚未真正落地
- 尚无自动化测试，核心算法正确性无法持续回归验证

### 中优先级

- 历史、收藏、7 天回看缺少系统化验收
- 无障碍支持不足
- 页面性能仍有优化空间

### 低优先级

- 补丁脚本仍保留在仓库中，需后续整理归档
- 文档部分内容仍需要持续校正

---

## 6. 建议计划

### 第一优先级：稳定可用

目标：先把“用户现在真的能用”做好。

- 收口 AI 解读可用性
- 统一返回体验
- 重做零基础白话引导
- 收敛 AI 文案并补明确下一步
- 完整验证收藏、历史、7 天回看
- 修正文档中的过度乐观描述

### 第二优先级：收口第一阶段

目标：让第一阶段从“有功能”变成“有闭环”。

- 补真实记录下的主题档案体验
- 补 7 天回看最终验收
- 补 AI 行动卡的展示一致性
- 补结果页行动引导与回看提醒

### 第三优先级：开始工程化

目标：为后续迭代减轻技术债。

- 拆分 `index.html`
- 抽离数据文件
- 给算法与存储层补最小测试

### 第四优先级：进入第二阶段能力建设

- 今日一问
- 月度回顾
- 多层解读模式
- 节气内容

---

## 7. 结论

当前项目不应再对外描述为“全部功能完成”。

更合适的表述是：

**观心已经完成 MVP 主体验，正在进行第一阶段功能收口，当前优先修复 AI 可用性、返回体验、零基础引导和结果页行动闭环。**

---

## 8. 本轮增量更新（2026-06-23）

### 8.1 页面与功能更新

- `命宫解读页`
  - 首屏文案继续收口，明确改为“命宫看性格与情绪底色、生日本命卦看长期主题、流年看今年节奏”。
  - “先记住这句”已改为“你现在最该留意的是”。
  - 左上返回按钮已从“回首页”改为“回上一页，兜底回首页”。
  - 分区小标题已从英文展示词改为中文任务型表达。
- `八星性格镜`
  - 首层卡片从长段说明压缩为短句型星性标签。
  - 深读层继续保留五行、现实场景、今天怎么用和边界提醒。
  - 顶部说明已改为“星性磁场”角度，弱化吉凶感，强化知识感。
- `摇签翻转卡`
  - 翻转卡正反面已改为同尺寸叠层。
  - 正面内容已放入独立滚动层，减少翻转后位移风险。
  - 正面标题与分区文案已改为更短、更直接的结果式表达。

### 8.2 当前验证状态

- `index.html` 内联脚本语法检查通过。
- 由于当前环境策略限制，未能在本轮内通过后台启动方式完成 `http://127.0.0.1:8080/` 的 HTTP 回归验证。
- 下一步优先在浏览器中验收三项：命宫页返回、八星首层阅读压力、摇签翻转是否真正原地翻面。

### 8.3 八星互动盘与数字说明接入（2026-06-23）

- `八星性格镜`
  - 新增“八方互动盘”，中心显示用户命宫数，外围八方节点显示“组合数 + 八星名”。
  - 节点支持点击与悬停联动，下方信息面板实时显示：先天卦数、方位落数、组合数、五行与星性。
  - 新增“看数字怎么用”入口，补充先天八卦数、落盘换算和组合数示例说明。
- `数字说明`
  - 明确区分“先天八卦数”和页面里实际用于配对的“八宅盘落数”。
  - 补充示例：命宫数为 1 时，`19 = 延年`，`16 = 六煞`，帮助用户理解读法。
- `卡片排版`
  - 八星卡片改为更规整的两层结构：星名/五行方位一层，星性短句一层。
  - 解决此前星名、方位、说明挤在一起导致难读的问题。

### 8.4 首页入口页与全局返回 UI 收口（2026-06-23）

- `全局返回`
  - 关键功能页顶部返回栏已统一升级为粘性顶栏。
  - 左上返回按钮从小方块改为更明确的胶囊按钮，触达面积更大，滚动后仍可见。
  - 顶栏加入半透明纸感背景，减少页面滚动后“找不到返回”的问题。

- `首页 / 观心功能页`
  - 首页从“功能卡片列表”重构为“主路径入口页”。
  - 标题区新增主路径提示：先填生辰、再看命宫与生日本命卦、再学卦或问事。
  - 三个主功能改为更明确的产品入口卡：命宫解读、每日一卦、以事问卦。
  - 卦象图鉴、数字起卦、占卦历史、重新排盘下沉为次级探索区，降低首页首屏噪音。
  - 首页设置入口已从右上角小齿轮改为带文字的“设置”按钮。

- `视觉与交互`
  - 首页结构去掉 emoji 式功能图标，改为更克制的文字印记和箭头语言。
  - 主功能卡新增“主路径 / 学习入口 / 问题入口”层级提示，降低新用户理解成本。
  - 快捷入口补充一句用途说明，不再只剩一个名词按钮。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。

待验收：

- 需要在浏览器中确认首页首屏信息层级是否明显优于旧版。
- 需要验证顶部返回按钮在命宫页、结果页、图鉴页滚动后是否仍然顺手。

### 8.5 八卦互动盘与主路径衔接修正（2026-06-23）

- `八卦互动盘`
  - 修正了互动盘节点定位 class，之前外围方位节点没有真正落到八方位置，导致看起来像“点击无效”。
  - 互动盘已改为以后天八卦数为主口径展示，方位节点直接显示对应后天卦位与八星。
  - 面板说明从“先天卦数 / 方位落数”调整为“后天卦数 / 方位卦位”，避免继续混淆八星配对逻辑。
  - 保留先天转后天的换算说明，但退到辅助说明层，不再作为主展示口径。

- `建档后的主路径引导`
  - 用户填写完生辰并生成档案后，命宫页顶部新增“建档已完成，下一步回主路径首页”的明确引导卡。
  - 引导卡提供“进入主路径首页”和“先继续看个人解读”两个动作，避免用户停在档案页里不知道下一步。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。

待验收：

- 需要在浏览器中确认八方节点已真实显示在盘面外围，而不是只剩中心圆和下方信息卡。
- 需要逐个点击至少两个方位，确认高亮态和下方面板文案会同步变化。
- 需要重新走一次“开始探索 -> 填生辰 -> 推演命理”流程，确认命宫页顶部出现主路径引导卡。

### 8.6 八宅星位逻辑与后天盘朝向修正（2026-06-23）

- `八宅星位逻辑`
  - 修正了 `生气 / 天医` 的映射关系，`68` 现已正确对应 `天医`。
  - 已用本地脚本回归验证：`calcBaZhai(6)` 中 `dirLuo=8` 返回 `star=ty`。

- `后天八卦盘`
  - 互动盘已改成 `离上坎下` 的后天八卦朝向。
  - 八方节点从卡片式按钮改为更轻的八卦符号节点，只通过颜色和激活态区分吉凶与当前选中。
  - 节点文案压缩，不再在盘面上堆“方位 + 星名”长标签，减少视觉拥挤。

- `主路径引导卡`
  - 建档完成后的引导卡文案已进一步精简，只保留“回首页继续主路径，或先留在这里看解读”的最小信息。

已验证：

- `68` 已校正为 `天医`。
- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。

### 8.7 手机端启动按钮触发加固（2026-06-23）

- `启动链路`
  - 针对手机端打开线上地址时，“开始探索”偶发需要点击多次的问题，已对启动按钮触发链路做一轮加固。
  - 首屏 `开始探索` 不再只依赖 `onclick`，额外绑定了 `pointerup + touchend`。
  - 新手引导最后一步的 `开始探索` 也同步采用同样的触发方式。
  - 启动按钮已补 `touch-action: manipulation` 与轻量防连点节流，减少内嵌浏览器首次点按只高亮不触发的概率。

- `当前判断`
  - 这次修复优先针对移动端 WebView / 手机浏览器的点按触发兼容性。
  - 线上是否彻底解决，还需要在真实手机环境继续验收；如果仍复现，下一步要继续排查是否为 GitHub Pages 未部署到最新版本，或是否存在首屏资源延迟导致的交互阻塞。

已验证：

- `index.html` 内联脚本语法检查通过。
- 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。

待验收：

- 需要在真实手机环境访问 `https://sskks.github.io/guanxin/`，确认首屏 `开始探索` 是否可一次触发。
- 需要确认新手引导里的最终 `开始探索` 也不存在多次点按问题。

### 8.8 八卦五行显色与八星区排版修正（2026-06-24）

- `八卦互动盘`
  - 修正了八卦节点“数据已带五行色，但界面几乎无变化”的问题。
  - 现在五行颜色直接作用于卦符号、卦名字样和节点辅助字，不再被内部固定字体颜色覆盖。
  - 节点 hover / active / pressing 态同步接入五行色光晕，按压放大反馈更明确，但仍保持原位，不引发布局偏移。

- `八星性格镜排版`
  - 收紧了“四个助力 / 四个提醒”的标题层级、说明字距和卡片正文行距。
  - 调整星卡内“星名 / 五行方位 / 简述”的节奏，减少上一版文字挤在一起、扫读困难的问题。

- `当前目标`
  - 下一步继续基于真实渲染结果验证八卦盘显色是否足够明显。
  - 如仍显得不够直观，再继续增强卦符号本体与底层盘面的五行映射，而不是只改说明面板。

### 8.9 八卦卦色与星色拆分（2026-06-24）

- `问题校正`
  - 之前八卦盘上方节点误用了“八星五行”来着色，和用户希望的“按卦本身五行着色”不一致。
  - 这会导致盘面语义混乱：上方看起来像卦位，实际却在显示星性颜色。

- `本轮处理`
  - 上方八卦节点现改为读取后天卦位自身五行：乾兑金、离火、震巽木、坎水、艮坤土。
  - 下方面板的“组合数 / 五行 / 星性”继续保留八星五行，用于表达八星性格镜的信息。
  - 按压态层级已修正，避免 `is-active` 把 `is-pressing` 的放大动画覆盖掉。

- `待继续验证`
  - 需要在真实页面确认上方卦色和下方星色已经明显区分。
  - 若按压放大仍不够显著，下一步继续增强节点缩放幅度与背景回弹节奏。

### 8.10 回访路径与八卦按压反馈修正（2026-06-24）

- `八卦节点反馈`
  - 在长按态之外，额外补了一层点击脉冲放大，保证用户即使只是点一下，也能明显感知节点被激活。
  - 目的不是做炫技动画，而是解决“代码有按压态，但用户实际看不见”的体验缺口。

- `回访路径`
  - 确认“命宫页适合首次建档后的引导，但不适合作为日常回访默认落点”。
  - 当前已将启动主路径改为：未建档用户进入生辰页，已建档用户再次进入时默认回首页。
  - 首页后续将承担日更内容入口，而命宫页保留为用户主动回看个人底色时再进入。

### 8.11 每日一卦短层与首页日更闭环（2026-06-24）

- `每日一卦页`
  - 已新增“今日先看这一层”短层板，位于卦辞、象辞之前。
  - 短层板固定为：一句判断、今天可做、今天先别急、晚上回看。
  - 目标是先帮用户抓住今天的重点，再决定要不要继续阅读传统文本。
  - 每日页底部重复返回按钮已移除，只保留左上角返回按钮。

- `首页今日状态板`
  - 已改为复用每日短层逻辑，不再直接显示长段解读文案。
  - 今日一卦卡现在显示“今天先看哪一层”，而不是泛化摘要。
  - 保存今日练习后，首页今日练习卡和首屏摘要会即时同步更新。

- `今日练习机制`
  - 新增从每日一卦页直接“记下今天练习”的入口。
  - 今日练习增加按日期校验，避免隔天仍展示昨天的旧动作。
  - 已修正首页练习卡“晚上回看”前缀重复的问题。

- `当前已验收`
  - 浏览器实测：首页首屏已真实展示今日状态板。
  - 浏览器实测：每日一卦页已真实展示短层板。
  - 浏览器实测：点击“记下今天练习”后，首页练习卡可立即更新为当日动作。

### 8.12 术数主链路正式收口（2026-06-24）

- `已完成`
  - 文档层正式确认：`八卦为体，梅花为用，命宫 / 生日本命卦为人，五行为翻译层，节气为留存层`。
  - 明确八星八宅继续保留，但只作为副层观察模块，不再承担主底层术数角色。
  - PRD 中“八宅风水”目标已调整为更贴近产品定位的“八星性格镜（当前由八宅盘承载）”。

- `本轮没有页面运行时改动`
  - 当前仅完成战略与文档口径收口，界面与交互改版留在下一轮实现。

### 8.13 命宫入口化与每日深读分层（2026-06-24）

- `命宫页`
  - 页面标题已从偏结果感的“命宫解读”收口为“命宫档案”。
  - 顶部导语改为“先看个人底色，再按需进入八星磁场与深读”，明确主次层级。
  - 顶部 flow tab 已从三段改为四段：`先看底色 / 看现实 / 八星磁场 / 去深读`。
  - 八星模块已从首个默认分段中拆出，单独进入“八星磁场”分段，减少首屏信息拥挤。

- `每日一卦页`
  - 页面不再直接常驻显示卦象图解、卦辞、象曰、长解释。
  - 新增“再看卦义深读”入口卡，首屏只保留今日短层和两个动作：`进入卦义深读 / 去图鉴看此卦`。
  - 深读内容改为通过统一弹层按需打开，包含卦象图解、卦辞、象曰、白话抓手与补充说明。

- `口径统一`
  - 运行时页面注释与文档中的“八宅风水页”已开始切换为“八星磁场页”口径。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.19 生日本命卦口径补清与解释升级（2026-06-24）

- `术数口径补清`
  - 当前“生日本命卦”已在页面解释和文档中明确为一套“生辰四柱天干 -> 纳甲数 -> 天数 / 地数 -> 上下卦 -> 六十四卦”的计算口径。
  - 已明确说明这层不是八宅命卦，也不是完整八字十神排盘或紫微斗数命盘，主要用于观察长期反复出现的处事主题。

- `页面内容升级`
  - 本命卦弹层标题统一收口为“生日本命卦”，减少与八宅“命卦”的混淆。
  - 本命卦弹层新增“它是怎么算出来的”“它到底在看什么”“它不是什么”“你的计算过程”等内容，不再只给笼统性格结论。
  - 术语说明中的“本命卦”解释同步改写为更清楚的来源与边界说明。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.20 生日本命卦从“特性词”改为“卦义解读” （2026-06-24）

- `内容结构调整`
  - 生日本命卦弹层不再强调算法说明，主结构改为“这卦本身在说什么 / 你通常怎么进入这类局 / 事情会把你推向哪里 / 最容易失衡的地方 / 你真正要练的功课”。
  - 解释重点从几个重复关键词，改成上下卦关系、卦的长期主题、现实中的反复处境与成长课题。

- `术数表达升级`
  - 本命卦不再只被解释成“长期性格地图”，而是明确解释为“长期反复会遇到的主题卦”。
  - 新增上下卦联读逻辑：先看你通常怎样起心，再看现实会把你推向哪一课。
  - 补充“容易失衡处”和“真正要练的功课”，避免只说优势或抽象气质。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.21 生日本命卦补入专属卦义模板（2026-06-24）

- `内容深度继续补强`
  - 生日本命卦不再只依赖上下卦通用拼接模板，新增一批常见主题卦的专属解释。
  - 当前已补入：`屯 / 蒙 / 履 / 泰 / 否 / 剥 / 复 / 咸 / 恒 / 困 / 井 / 渐`。
  - 解释重点继续围绕：`这卦本身在说什么 / 你通常怎么进入这类局 / 事情会把你推向哪里 / 最容易失衡的地方 / 真正要练的功课`。

- `表达口径调整`
  - 本命卦内容减少“重复特性词”式写法，改为更贴近卦本身的处境、次序和修炼重点。
  - 对 `剥 / 困 / 井 / 复` 这类更容易击中用户现实感受的卦，补入“旧结构剥落 / 困后返源 / 回头复位 / 养井修源”等更明确的周易主轴。

- `待继续验证`
  - 已完成代码层语法修正，下一步需在页面中继续验证“生日本命卦深读”入口与弹层展示是否顺畅。

### 8.22 八星入口辨识与本命卦去重（2026-06-24）

- `八星磁场交互修正`
  - 八卦盘下方原来的“看星性说明”按钮已改成更明确的展开入口样式。
  - 文案改为 `打开星性说明 / 学习数字怎么用`，并补了辅助提示，避免用户误以为这是已选中状态或不可点击标签。
  - 按钮视觉已从普通浅色按钮改成更像“继续进入下一层”的卡片式入口，移动端会自动纵向堆叠。

- `生日本命卦内容去重`
  - 删除本命卦弹层里与摘要重复的 `一句判断` 步骤。
  - 收掉与步骤内容高度重合的“上下卦怎么一起看”补充段，保留更必要的：来源、白话抓手、失衡点、练法、边界、传统文本。
  - 当前目标是让用户一层一层读，不再在同一个弹层里反复看到同义表达。

### 8.23 阶段重心切换确认（2026-06-24）

- `当前判断`
  - 现阶段文本内容已基本达到当前版本可用线，下一步不再继续大幅扩写传统文化解释。
  - 开发重心正式转向：`体验闭环 / 交互效率 / 回访理由 / 工程准备`。

- `下一步顺序`
  - 优先收口：命宫档案页与八星磁场页交互。
  - 其次强化：每日一卦与今日练习的回访闭环。
  - 然后整理：问卦结果页、历史收藏闭环、节气留存层最小版本。
  - 最后进入：单文件架构的拆分准备与最小回归检查。

### 8.24 首页回访层、结果页折叠层与历史洞察面板（2026-06-24）

- `首页更新`
  - 首页首屏新增“为什么今天回到这里”的回访提示条，用来区分 `七日回看 / 今日练习 / 普通回访` 三种状态。
  - 今日练习卡下方新增节气卡，展示当前节气、距离下一个节气的天数，以及“今天更适合怎么用这层提醒”。
  - 节气卡支持点开查看轻量说明，明确它只承担“顺时而看”的背景提醒，不替代命宫档案或问卦结果。

- `问卦结果页更新`
  - 首屏继续收口为“结论 + 可执行动作 + 下一步入口”。
  - 互卦、变卦、卦辞、建议动作、AI 深读改为折叠层承载，避免结果页再次回到长页面堆叠模式。
  - 每次重新进入结果页时，折叠层会自动复位为收起状态，保证用户先看到重点。

- `历史页更新`
  - 历史页新增洞察面板，不再只有记录列表。
  - 面板会根据当前记录集合提炼：最近更常问的主题、收藏占比、近期问题关键词，帮助用户先看“自己最近在反复经历什么”，再看单条历史。
  - 当筛选到某一主题或收藏集合时，洞察面板会跟随当前结果同步变化。

- `八星磁场页更新`
  - 八星页顶部说明继续压缩为三步式引导：先点方位、再看星性、最后学数字怎么用。
  - 目的不是再增加解释，而是降低新用户第一次进入时的信息负担。

- `当前验证`
  - `index.html` 内联脚本语法检查已通过。
  - 首页节气卡初始化已接入 `DOMContentLoaded`，重新进入首页即可看到首屏回访层完整结构。

### 8.25 首页首屏、节气建议卡与历史洞察卡重做（2026-06-24）

- `首页首屏`
  - 删除了首屏“01 / 02 / 03”式主路径提示，避免一打开就像在看产品内部结构说明。
  - 首页副标题改短，状态板主标题改成更直接的入口语气：先定方向，再决定走哪条路。
  - 回访提示条从胶囊标签改成整条说明卡，减少“信息很多但抓不住重点”的感觉。

- `节气卡`
  - 节气卡从“节气留存”改成“今日节律建议”。
  - 卡片新增 `公历日期 + 干支日 + 五行主调` 展示，不再只给一个模糊节气名。
  - 内容改成三类实际建议：`穿衣颜色 / 情绪节奏 / 今天先做什么`。
  - 详情弹层同步改成“今天怎么用”，不再以抽象背景说明为主。

- `历史页`
  - 删除“最近问题标签”那组测试感很重的红色问题 chip，避免页面像调试态。
  - 洞察卡改成三项更稳定的信息：`最近最常问 / 反复出现的卦 / 已收藏`。
  - 月报卡从“数字陈列”改成更像产品摘要卡的结构，补一行解释说明“这个月的提问概况”。

- `当前判断`
  - 这一轮重点不是加功能，而是把已有信息改得更短、更像真实产品页。
  - 还需要下一轮继续收首页首屏文案密度，以及节气建议与真实天气的进一步结合。

### 8.26 问卦结果页改成“今日落点 + 继续追问此卦” （2026-06-24）

- `结果页结构调整`
  - 原来的 AI 行动卡与 AI 全文存在明显重复，本轮已把结果页改成两层：
    - 首层：`今日落点`
    - 第二层：`继续追问此卦`
  - `今日落点` 现在只保留三件事：`此刻判断 / 先做什么 / 先别急什么`，作为用户打开结果页第一眼应该看到的重点。

- `AI 角色调整`
  - AI 不再承担“再把整卦讲一遍”的职责。
  - AI 改成“继续追问此卦”的主题追问入口，只允许围绕当前卦、本卦动爻、互卦、变卦来回答。
  - 追问区已加入主题快捷入口：`感情 / 事业 / 钱财 / 身心 / 该不该主动 / 现在该推进还是缓一缓`。
  - 同时保留一个自定义输入框，但回答仍然被限制在当前卦范围内解释。

- `风险控制`
  - AI 追问不再输出“你应该分手 / 辞职 / 投资”这类命令式断语。
  - 回答被固定为四段：`卦里怎么看 / 放到这个主题里 / 今天先怎么做 / 边界提醒`。
  - 目的是让 AI 继续做传统文化翻译层，而不是变成高风险决策替代品。

- `手机端体验`
  - 追问入口改成大触达按钮网格，适合手机单手点按。
  - 自定义问题输入框与结果块都收成短卡片结构，减少纵向阅读疲劳。

- `当前验证`
  - `index.html` 内联脚本语法检查通过。

### 8.18 首页语言收口与困卦深读加强（2026-06-24）

- `首页体验`
  - 首页继续清理偏“内部思考式”的提示语，改成用户更容易直接读懂的说法，如“今天可以从这里开始”“先看自己”“每天看一卦”。
  - 今日练习卡新增可见时间轴：`已记下 -> 待回看 -> 已完成`，并且会随状态联动高亮。
  - 今日练习状态说明去掉系统口吻，改成更贴近日常使用场景的提示语。

- `卦义内容`
  - `泽水困` 的每日一卦摘要、今日行动、回看提醒已改为卦专属表达，不再使用泛化模板句。
  - `泽水困` 深读补入“升 -> 困 -> 井”的次序说明，强调困后不是硬冲，而是复源、养水、整顿根本。
  - `困卦` 爻辞白话解释已重写为更贴近本义的处境描述，减少空泛转述。

- `测试环境`
  - 已在当前 `http://127.0.0.1:8080/` 页面清理本地 `localStorage` 与 `IndexedDB`，便于从干净状态重新测试。
  - 已确认首页可见新的入口标题与“待回看”时间轴节点。

### 8.14 命名统一与文案压缩（2026-06-24）

- `页面命名`
  - 首页主入口、启动页提示、档案内联入口里的“命宫解读”已统一收口为“命宫档案”。
  - 八星弹层标题已从“八星性格镜”收口为“八星磁场”。
  - PRD 与 README 中面对用户的功能名称同步更新，避免页面和文档口径继续分裂。

- `阅读负担`
  - 每日一卦深读入口卡文案再压短，减少解释性重复。
  - 命宫档案页“今天先这样用”“八星磁场页”“现实维度”三处说明文字继续收短，优先保留行动和判断层。

- `当前状态`
  - 这一轮仍以单文件 `index.html` 为运行入口，没有引入新依赖，也没有拆文件。
  - `index.html` 内联脚本语法检查通过。

### 8.15 命宫入口化强化与每日深读重排（2026-06-24）

- `命宫档案页`
  - 首屏新增三个直接入口动作：`命宫底色 / 本命卦主题 / 流年节奏`，把原本要往下读的核心内容前置成更明确的点击路径。
  - “先看底色”分段新增三张导流入口：`先看命宫解释 / 再看本命卦 / 直接去现实维度`，减少用户被长段正文拦住的情况。
  - 这轮目标不是再堆解释，而是把命宫页更明确地做成“个人底色入口页”。

- `每日一卦深读`
  - 深读弹层从连续段落改成分段阅读结构：`先看卦象 / 再读原文 / 最后落回今天`。
  - 弹层新增“这卦先抓哪一层”“先看上下卦在说什么”“卦辞原文”“象曰原文”四块内容卡，比之前整段铺开更适合手机阅读。
  - 深读入口按钮宽度同步做了移动端适配，减少按钮长短不齐的问题。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.16 命宫页去重与八星数字用法重构（2026-06-24）

- `命宫档案页去重`
  - 已删除首屏顶部重复的三张导流卡，避免和“为什么会这样”下面的入口再次讲同一件事。
  - `看现实` 分段不再重复讲一遍命宫底色和场景说明，改成直接进入事业 / 感情 / 身心 / 钱财四维入口。
  - `去深读` 分段已明确改成“看原文”，只承担术语、原文和长解释，不再重复当天结论。

- `八星磁场页重构`
  - 八星页的说明已从“泛泛吉凶”改成先讲星性本身：它更容易把人带进什么状态，失衡时怎么表现，现实里如何校正。
  - 八星按钮文案从“看完整解释”改成“看星性说明”，避免用户误以为这里只是另一层重复解读。
  - 八星传统描述 `BZ_STAR_DESC` 已统一改成更中性、可用于现实观察的表述。

- `数字怎么用`
  - “看数字怎么用”已改成独立教学弹层，教用户如何用后天八卦数观察手机号尾号、工位号、门牌号等反复出现的数字。
  - 已删除八星页中的先天记忆法，只保留后天八卦数读取方式。
  - 使用边界已明确：数字只作状态提醒，不作吉凶断语。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.17 今日练习状态反馈闭环（2026-06-24）

- `反馈机制重做`
  - “记下今日练习”不再只是弹一次 toast，而是会在首页保留一张有状态的任务卡。
  - 当前已建立 4 种状态：`已记下 / 待今晚回看 / 已完成 / 待调整`。
  - 用户点击 `今晚再回看` 后，按钮会变化为已设回看，卡片继续保留；后续还可以补点 `我做到了` 收成完成态。
  - 用户点击 `我做到了` 后，卡片会切换为完成态，按钮进入不可重复点击状态，避免重复操作。

- `首页可见变化`
  - 今日练习卡新增状态徽标、状态说明、按钮文案变化和完成后样式变化。
  - 首页顶部“今天先这样用”摘要也会跟随练习状态一起变化，不再只显示静态动作句。

- `适配理由`
  - 这样比“只点一次按钮就消失”更适合手机用户回访，也更符合你要的“白天先做，晚上回看”的节奏。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.27 首页节气卡与结果页追问区第三轮收口（2026-06-24）

- `首页首屏继续去说明板化`
  - 首页副标题从“功能解释”继续收短，改成更像用户入口的话术。
  - 今日状态板默认标题已从“先定方向，再决定走哪条路”收口为“先看今天这一层，再决定往哪走”。
  - “今天为什么回来”的几种状态文案同步改短，减少替用户下判断式的表达。

- `节气卡逻辑明确化`
  - 首页节气卡删除了多余的“看今天怎么用”按钮，避免和卡片正文重复。
  - 新增“依据说明”短段落，明确当前规则为：`日支定今日主五行，节气只作时令环境修正`。
  - 节气弹层中的说明同步改写，避免用户误以为这层内容是在直接断事。

- `结果页去重`
  - 结果页首屏从三张并列信息卡收为“一句落点 + 两张动作卡”，减少首屏卡片堆叠感。
  - `今日落点` 入口文案已改为更中性的 `先看这一层`，弱化死板感。
  - AI 追问区删除单独的“追问边界说明”卡，并把边界提示并入结果结构与追问模式提示里。
  - AI 追问区顶部说明、输入框标签与按钮区文案继续压短，减少重复解释。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.28 命宫档案 / 八星磁场交互收口第一轮（2026-06-24）

- `命宫档案页路径化`
  - 命宫页分段导航下方新增当前路径状态条，明确用户此刻是在看“个人底色 / 现实维度 / 八星磁场 / 原文深读”中的哪一层。
  - 命宫页渲染完成后会主动把当前状态重置到“先看底色”，避免多次进出后停留在旧分段造成迷路。

- `八星磁场页联动加强`
  - 后天八卦盘下方新增“当前看到的是哪颗星”的即时提示，不再只靠用户自己理解当前选中状态。
  - 当盘面切到某个方位时，下方“四个助力 / 四个提醒”里的对应星卡会同步高亮，增强盘面与列表的联动感。
  - 盘面动作区现在更明确地告诉用户：当前选中了哪颗星，下一步可以直接展开星性说明。

- `弹层手机体验补强`
  - 解释弹层顶部新增拖拽感手柄，底部新增固定“看完了，返回上一层”按钮。
  - 目标是减少用户上下滚动后找不到关闭入口的问题，让回退路径更稳定。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.29 命宫档案 / 八星磁场交互收口第二轮（2026-06-24）

- `星性说明去重`
  - “打开星性说明”弹层不再把主页面里的 `星性底色 / 现实场景 / 今天怎么用` 再讲一遍。
  - 弹层现在只保留三类补充：`现实里常怎么出现 / 失衡时先看哪里 / 先做的小动作`。
  - 长说明部分收口为两段：`为什么你会在这里看到它` 与 `传统原意`，去掉重复的八宅边界、卦象关联等层。

- `四分段入口显性化`
  - 命宫页原来的四个细小胶囊按钮，已改成更明显的双列导航面板。
  - 顶部新增“从这里切换四层内容”说明，四个入口分别附带一行白话用途，降低用户“不知道四个分段在哪里”的问题。
  - 当前状态条文案同步改成 `当前在看：... / 第 n 层`，比原来的“第 n 步”更清楚。

- `本轮保留`
  - 八星盘当前选中提示、列表联动高亮、弹层顶部手柄和底部返回按钮继续保留，不回退上一轮有效交互。

- `已完成验证`
  - `index.html` 内联脚本语法检查通过。

### 8.30 P0 / P1 回访闭环补齐（2026-06-24）

- `结果页联动`
  - 问卦结果页新增 `记下这次今日动作` 按钮，可把当前卦里的行动提醒直接写入首页今日练习卡。
  - 结果页在以下三种场景下都会同步恢复按钮状态：重新打开当前结果、从历史记录回看、收藏后回到结果页。
  - 若当前动作已写入首页，结果页按钮会自动改为 `今日动作已记下`，避免重复保存感过弱。

- `首页今日练习闭环`
  - 首页今日练习卡新增 `已记下 -> 待回看 -> 已完成` 小时间轴，状态与按钮文案实时联动。
  - `今晚再回看` 会把练习推进到待回看态；`我做到了` 会推进到已完成态。
  - 首页顶部今日状态板也会根据练习状态同步改写摘要，不再停留在静态提示。

- `节气留存层`
  - 首页节气卡新增 `展开节律说明` 入口，用户可以从首屏摘要进入完整节律说明层。
  - 节气层继续保持“日支定主五行，节气定环境节奏”的口径，不扩展成复杂历法工具。

- `历史回看页`
  - 历史列表改为按天分组展示，同一天多次问卦会聚合在同一日期分组下。
  - 记录补上 `已回看`、`有 AI 追问` 两个短标签，帮助用户区分哪些记录已经形成后续动作。
  - 这一轮没有新增删除历史功能，历史清理仍是后续迭代项。

- `当前验证状态`
  - `index.html` 内联脚本语法检查通过。
  - 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
  - 关键接线字符串已确认存在：`记下这次今日动作`、`展开节律说明`、`已回看`、`有 AI 追问`。

- `当前仍待验收`
  - 需要在真实浏览器中逐条验收结果页保存动作、首页练习时间轴、历史分组与节气弹层的可见交互。
  - 由于本轮环境未直接拿到可用浏览器控制链路，渲染态验收仍需你本地点击核对，不能把它算成已视觉验收通过。

### 8.31 结果页去重、AI 分层与节气入口收口（2026-06-24）

- `结果页首屏`
  - 已去掉 `三句话总结` 这一层，避免和 `先看卦在说什么 / 今日落点` 重复。
  - 首屏目前保留三块信息：白话解释、今日落点、下一步动作。
  - `AI 追问全文` 折叠块已移除，避免和结构化 AI 追问结果重复。

- `AI 追问入口`
  - 当前逻辑已改成：默认先显示 `先看 AI 解读`，只有点过一次后才展开主题追问网格和输入框。
  - 这样可以减少用户“我还没问，为什么已经在继续追问”的错位感。

- `导航与即时反馈`
  - 从占卦结果页返回时，已补上跳过 `bamboo` 摇签动画页的回退逻辑。
  - 收藏当前卦象、历史页点星标、七日回看写入 `reviewedAt` 后，当前内存记录会同步刷新，不再需要重进页面才看到状态变化。

- `首页节气卡`
  - 已从首屏大说明卡收成较轻的节律入口卡。
  - 节气的详细解释、边界与步骤继续保留在弹层里，不再与首页主卡平权堆叠。

- `当前验证状态`
  - `index.html` 内联脚本语法检查通过。
  - 本地 `http://127.0.0.1:8080/` 返回 HTTP 200。
  - 已人工核对首页截图：此前双大卡问题真实存在，本轮已按这个可见问题收 UI。

- `当前仍待验收`
  - 需要你在真实手机/浏览器里再验收：首页首屏层级、结果页 AI 入口显隐、结果页返回是否跳过摇签页。
