# 观心开发计划

> 版本：v1.2
> 最后更新：2026-06-23

---

## 1. 计划原则

当前开发计划遵循四个原则：

- 先保证 `可用`
- 再保证 `可理解`
- 再保证 `可复访`
- 最后再扩展 `内容深度` 与 `社交可能性`

因此，未来一段时间不建议继续横向堆功能，而应优先：

- 收口现有体验
- 修复可用性问题
- 修复可理解性问题
- 验证第一阶段核心闭环
- 再启动真正的工程化拆分

---

## 2. 当前阶段判断

当前项目阶段：

**MVP 主体验已完成，第一阶段功能正在收口。**

当前最关键的问题不是“缺多少新功能”，而是：

- AI 是否稳定可用
- 用户是否能顺手返回上一页
- 零基础用户是否看得懂、知道如何开始
- AI 是否足够短、直、可执行
- 结果页是否明确告诉用户下一步
- 历史/收藏/7 天回看是否真的构成闭环
- 文档、实现和仓库结构是否一致
- 单文件架构是否开始进入可维护状态
- 新确认的“四维生活解析”和“术数表达三层法”是否真正落到页面

---

## 3. 总体阶段规划

### 阶段 A：稳定可用与可理解

目标：

**先让当前版本对用户来说真正可用，而且第一次就能看懂。**

核心任务：

- 收口 AI 解读可用性
- 统一返回按钮位置与返回逻辑
- 为关键页面补符合直觉的手势返回
- 把新手引导改成零基础白话引导
- 将 AI 解读改为“短结论 + 行动建议”优先
- 给结果页补明确下一步引导
- 验证收藏是否真实可感
- 验证历史记录是否稳定
- 验证 7 天回看是否能完整触发与标记
- 对齐 README / PRD / 状态文档与真实实现

完成标准：

- 用户可以稳定完成一次完整问卦与 AI 解读
- 关键页面返回入口统一且顺手
- 零基础用户首次使用不需要额外解释也能完成主流程
- 用户看完结果后知道接下来该做什么
- 收藏、历史、回看三者可连贯使用
- 文档不再夸大当前完成度

### 阶段 B：第一阶段闭环完成

目标：

**让“情绪 → 启发 → 行动 → 回看”真正成立。**

核心任务：

- 优化 AI 行动卡表达和展示层级
- 落地四维生活解析（事业 / 感情 / 身心 / 钱财）
- 将命宫、本命卦、流年、八宅内容改写为术数表达三层法
- 让历史主题筛选真正服务用户理解自己
- 增强 7 天回看内容质量
- 提升首页回访理由的可见性
- 让结果页行动区与回看机制联动

完成标准：

- 用户不是“看完就走”，而是知道下一步怎么做
- 四维解析既保留术数依据，也能让零基础用户看懂
- 用户历史页中能看见自己的主要困扰主题
- 回看机制能帮助用户感知自己变化

### 阶段 C：工程化整理

目标：

**为后续长期迭代减轻技术债。**

核心任务：

- 拆分 `index.html`
- 提取卦象与命理数据
- 梳理目录结构
- 设计最小测试方案
- 减少未来修改单点风险

完成标准：

- 主应用不再完全依赖超大单文件
- 数据与视图初步分离
- 至少核心纯函数可做回归验证

### 阶段 D：内容深度建设

目标：

**让观心从功能型产品升级为内容型情绪成长产品。**

核心任务：

- 今日一问
- 多层解读模式
- 节气专题
- 月度回顾
- 人生场景图鉴增强

完成标准：

- 用户会因为内容本身回来，而不只是因为问卦
- 产品开始形成稳定的内容气质

### 阶段 E：轻社交验证

目标：

**在不破坏私密感和克制感的前提下，验证分享和共鸣是否成立。**

核心任务：

- 分享卡片 2.0
- 好友互占增强
- 一句回应
- 匿名主题观察

完成标准：

- 分享有真实传播场景
- 互动保持轻量，不把产品推向高噪声社区

---

## 4. 近期开发优先级

### P0：立刻推进

#### 1. AI 可用性稳定

为什么先做：

- 它是结果页最强感知能力之一
- 一旦不可用，用户会直接认为产品“坏了”

具体事项：

- 保证本地 Key 路径可用
- 保证代理兜底路径可用
- 优化错误提示文案
- 明确设置页使用说明

验收标准：

- 有 Key 时可稳定返回内容
- 无 Key 时提示清晰
- 错误时不出现技术性强、用户看不懂的提示

#### 2. 主路径导航与返回体验

为什么先做：

- 返回是所有页面的基础安全感
- 情绪类产品里，迷路和不敢点返回会直接降低信任

具体事项：

- 统一重要页面返回按钮到左上角
- 优先返回上一页，而不是默认返回首页
- 为关键页面补右滑返回上一页
- 明确页面层级，避免用户不知道自己在哪

验收标准：

- 用户单手也能快速找到返回
- 关键页面返回行为一致
- 滑动返回不会误触主交互

#### 3. 零基础新手引导重做

为什么先做：

- 当前最大流失风险不是功能缺失，而是看不懂和不会用

具体事项：

- 把功能导览改成“你现在适合点哪里”
- 给命宫、卦、爻、图鉴等术语补白话说明
- 在核心入口增加场景化提示
- 把“新手第一次该怎么用”做成最短路径

验收标准：

- 不懂周易的用户也能独立完成首次问卦
- 新手引导不依赖专业术语就能讲清主流程
- 用户知道不同入口分别适合什么问题

#### 4. AI 输出收敛与下一步引导

为什么先做：

- AI 现在的主要问题不是“有没有内容”，而是“没有重点、没有动作”

具体事项：

- AI 默认输出缩短为结论优先
- 优先呈现一句判断、一条提醒、一个行动
- 补“今天可以做什么 / 先不要做什么 / 何时回看”
- 压缩大段铺陈和重复解释

验收标准：

- 用户 5-10 秒内能抓到核心意思
- 每次解读都有明确下一步
- AI 文案整体更短、更直白、更不说教

#### 5. 收藏 / 历史 / 7 天回看验收

为什么先做：

- 这是第一阶段“复访理由”的核心

具体事项：

- 验证收藏即时反馈
- 验证历史页收藏筛选
- 验证主题条与搜索联动
- 验证 7 天回看触发与 `reviewedAt`

验收标准：

- 收藏后用户能立刻看见反馈
- 历史页记录能被正常筛选与打开
- 回看卡片出现、点击、标记流程完整

#### 6. 文档与状态维护

为什么先做：

- 当前项目已经进入需要长期协作和连续迭代的阶段

具体事项：

- 继续保持 PRD、状态页、架构文档同步
- 每完成一批功能，就更新已完成 / 进行中 / 待开发

验收标准：

- 文档不再和代码状态脱节

### P1：下一批推进

#### 7. 四维生活解析第一版

为什么先做：

- 这是刚确认的产品方向，直接对应用户最关心的事业、感情、身心、钱财。
- 它能把现有命宫、生日本命卦、流年、八宅、五行内容重新组织成用户真正能消费的结构。

具体事项：

- 在命宫解读页新增四维解析区。
- 每一维固定结构：一句判断、一个卡点、一个现实核对、一个今日练习、一个传统根据、一个边界提醒。
- 默认先做静态生成，不依赖新增排盘系统。

验收标准：

- 用户一眼能区分四个维度分别在看什么。
- 每一维都有现实落点，而不是只换一种说法重复命宫。
- 敏感维度边界完整可见。

#### 8. 术数表达三层法落地

为什么先做：

- 当前文案最大风险不是“没内容”，而是要么太软太虚，要么太像断语。
- 三层法是内容质量的统一标准，必须尽快变成实现规范。

具体事项：

- 优先改写命宫解读页的命宫、本命卦、流年深读文案。
- 优先改写问卦结果页的白话先看、互卦、变卦、行动建议。
- 给 AI prompt 增加“术数骨架 -> 现实翻译 -> 情绪承接”的结构约束。

验收标准：

- 每段解读至少保留一个明确术数依据。
- 每段解读至少给一个现实核对点或行动点。
- 文案不出现明显鸡汤化或恐吓化两极问题。

#### 9. 真正开始拆分单文件

建议拆分顺序：

1. 抽出纯样式
2. 抽出纯数据
3. 抽出工具函数和存储层
4. 最后再抽业务模块

验收标准：

- 页面可正常运行
- 拆分后行为不回退
- 至少完成第一轮“样式 + 数据”分离

#### 10. 为核心算法补最小测试

优先测试对象：

- 数字起卦
- 梅花易数起卦
- 主题分类
- 历史回看筛选逻辑
- 签到连续天数逻辑

验收标准：

- 最少建立一组可重复运行的验证脚本

### P2：中期推进

#### 11. 今日一问

价值：

- 从“有问题才来”扩展到“无问题也能来”

#### 12. 月度回顾

价值：

- 强化长期反思和用户成长感

#### 13. 多层解读模式

价值：

- 从单次解释扩展到不同深度的理解方式

---

## 5. 推荐执行顺序

建议按以下顺序推进：

1. AI 可用性收口
2. 主路径导航与返回体验统一
3. 零基础新手引导重做
4. AI 输出收敛与下一步引导
5. 收藏 / 历史 / 7 天回看完整验收
6. 四维生活解析第一版
7. 术数表达三层法落地
8. 单文件拆分第一步
9. 核心测试补齐
10. 今日一问
11. 月度回顾
12. 多层解读模式
13. 节气内容
14. 轻社交验证

### 5.1 当前执行批次：阶段 A / P0 收口

本批次目标不是扩展新功能，而是修复朋友测试暴露出的基础体验问题，让用户第一次使用时能顺利完成主流程。

执行切片：

1. 返回体验：统一关键页面左上返回，内部导航优先返回上一页，并在移动端关键页面支持右滑返回。
2. 零基础引导：把新手引导从“介绍功能”改为“告诉用户现在该点哪里”，并用白话解释卦、爻、命宫。
3. AI 输出：把 AI 解读改成一句结论、当前提醒、建议行动、今日避免、一句提醒、短详细解读。
4. 行动闭环：结果页先展示“先做这一件事”，并让 AI 建议覆盖默认行动提示。
5. 文档同步：每完成一个切片，同步 `docs/PROJECT_STATE.md`，避免文档和代码脱节。

当前代码状态：

- 1-4 已完成第一版代码接入。
- 5 已同步到项目状态文档。

当前验收重点：

- 浏览器实际交互：左上返回、右滑返回、结果页行动提示、新手引导弹窗。
- AI 实测：本地 Key 路径和后端代理路径至少跑通一种。
- 移动端视觉：375px 宽度下无横向滚动，按钮触达面积不小于 44px。

### 5.6 当前准备启动批次：四维解析与表达升级

本批次目标是把刚确认的理念真正落到产品，而不是继续只改文档。

执行切片：

1. 在命宫解读页加入四维生活解析第一版。
2. 先用现有命宫、生日本命卦、流年宫、八宅八星、五行关系生成四维内容，不引入新术数系统。
3. 改写命宫、本命卦、流年深读内容，统一采用术数表达三层法。
4. 调整 AI prompt 和结果页文案结构，让输出既保留术数骨架，又有现实翻译和情绪承接。
5. 为事业、感情、身心、钱财四维补边界提醒。

当前代码目标：

- 先完成命宫解读页第一版。
- 再同步问卦结果页的表达结构。

当前验收重点：

- 四维解析是否明显区别于原有命宫摘要。
- 文案是否保留术数依据，而不是被稀释成普通心理安慰。
- 感情、身心、钱财等敏感维度是否完整显示边界提醒。

### 5.2 二次修正后的验收顺序

最新反馈说明：只做“有返回、有白话、有行动卡”还不够，必须让用户明显感觉到页面在帮他理解和行动。

下一轮验收按以下顺序执行：

1. 命理档案页：用户是否能在顶部直接返回；是否能看懂命宫、本命卦、流年宫、五行生克；是否拿到一条现实建议。
2. 占卦结果页：返回是否只保留在左上角；“白话先看”是否比卦辞更先解释处境；“先做这一件事”是否足够具体。
3. 图鉴详情：从结果页点击“看懂这个卦”是否直接打开当前卦详情；详情页是否有白话解释和术语解释。
4. 分享能力：复制到微信是否不再是一行字；分享卡片是否突出启发和行动，而不是旧式信息堆叠。
5. AI 解读：生成结果是否能稳定输出可执行的小动作，而不是复述卦辞。

### 5.3 第三次修正后的验收顺序

本轮重点是避免“看似有解释，但实际重复或不够准确”。

下一轮优先验证：

1. 连续占卦：同一天连续问卦两次，不能再出现“今日已占”限制，两条记录都应进入历史。（已验证数字起卦路径）
2. 历史记录：同一天多条记录不能互相覆盖，收藏、打开详情、AI 结果恢复仍可用。（覆盖问题已修复，收藏/AI 回写已改为精确记录 key）
3. 本命卦准确性：命理档案页展示的本命卦详细解读必须来自当前用户真实计算出的本命卦，而不是固定文案或随机卦。
4. 命宫解读页：命宫、流年、本命卦三层信息要区分清楚，不能只讲命宫。
5. 图鉴入口：结果页负责“当下怎么理解和行动”，图鉴负责“传统结构和延展学习”，二者内容不能重复。

### 5.4 历史覆盖修复后的回归项

本轮已经完成历史覆盖专项修复，后续每次改动占卦、AI 或历史功能时必须回归以下项目：

1. 同一天连续两次数字起卦，历史页必须出现两条不同记录。
2. 同一天连续两次以事问卦，历史页必须出现两条不同记录。
3. 收藏第二条同日记录时，不能误收藏第一条。
4. 对第二条同日记录生成 AI 解读时，不能把 AI 结果写回第一条。
5. 分享链接打开后如果不是本机原始记录，不应把 AI 解读或收藏状态写入本地历史。

### 5.5 线上测试清单（2026-06-23）

本次提交后，线上测试优先按以下顺序进行：

1. 首次访问：启动页、免责声明、新手引导、生辰信息填写是否连贯。
2. 命理档案：命宫、本命卦、流年宫是否有白话说明和现实建议。
3. 以事问卦：输入问题、摇签动画、翻转卡片、完整结果页是否稳定。
4. 数字起卦：连续起两次，历史页是否出现两条独立记录。
5. 收藏反馈：点击收藏后是否有提示，历史收藏状态是否同步。
6. AI 解读：部署环境是否能跑通代理或 Key 配置，回复是否短、直接、有行动建议。
7. 图鉴跳转：从结果页进入当前卦图鉴时，是否精准定位到该卦详情。
8. 返回体验：关键页面左上角返回、移动端右滑返回是否符合预期。
9. 分享卡片：复制文字、保存图片、分享内容是否比旧版更适合微信传播。
10. 移动端视觉：375px 宽度下无明显错位、横向滚动或按钮过低问题。

---

## 6. 本阶段不建议做的事

当前不建议优先推进：

- 重社区
- 评论系统
- 关注系统
- 微社交扩容
- 复杂商业化体系
- 大范围视觉重构
- 继续大量新功能并行开发

原因：

- 容易进一步稀释当前主价值
- 会拖慢第一阶段收口
- 会让项目在工程上更难维护

---

## 7. 交付方式建议

后续每一轮开发，建议固定采用以下节奏：

### 7.1 开发前

- 明确本轮目标
- 明确是否改文档
- 明确验收标准

### 7.2 开发中

- 小步提交
- 每轮只解决一类核心问题
- 文档随进度同步更新

### 7.3 开发后

- 记录已完成
- 记录遗留问题
- 明确下一个最优先事项

---

## 8. 结论

当前最正确的推进方式，不是再横向扩张，而是：

**先把“观心”作为一个情绪类传统文化产品的主路径打磨清楚，让用户能返回、能看懂、能抓重点、知道下一步，再进入内容深度与长期成长方向。**
---

## 9. 最新执行进度（2026-06-23）

### 9.1 本轮已完成

- `命宫解读页`
  - 已接入“四维生活解析”第一版，放在“看场景”流中。
  - 已新增“事业 / 感情 / 身心 / 钱财”四张入口卡片。
  - 卡片点击后可打开统一深读弹层，内容结构为：一句观察、容易卡住、现实核对、今天行动、传统依据、使用边界。
- `术语统一`
  - 命宫页摘要区“本命卦”可见标签已继续统一为“生日本命卦”。
- `验证`
  - `index.html` 内联脚本语法检查通过。
  - 390px 移动宽度下已验证四维卡片真实渲染为 4 张，并可打开对应弹层。

### 9.2 下一步直接续做

- 压缩四维文案里的重复表达，减少“换句话重说”的感觉。
- 继续把“术数骨架 -> 现实翻译 -> 情绪承接”落到问卦结果页。
- 处理现有新手引导遮罩对深层页面交互的干扰，避免自动或真实点击被遮挡。

### 9.3 当前收口切片（2026-06-23）

- `命宫解读页可读性`
  - 继续压缩首屏话术，减少抽象词和展示型标语。
  - 固定“命宫 / 生日本命卦 / 流年”三层解释框架，降低新用户理解成本。
  - 把左上返回统一到“回上一页”逻辑。
- `八星性格镜分层`
  - 首层只展示短句型星性标签，解决“字太多、不想读”的问题。
  - 二层深读保留五行、失衡点、现实场景和边界说明，满足“边用边学”的需求。
- `摇签翻转卡稳定性`
  - 以“原地正反翻”为目标，统一正反面外框和滚动层，避免翻转后位移或消失。

### 9.4 本切片完成后优先验收

1. 命宫页首屏是否更像“现在该怎么理解自己”，而不是概念堆叠。
2. 八星首层是否一眼能读完，点开后仍有足够传统文化信息。
3. 摇签翻转是否已经接近“卡片原地翻面”的视觉要求。

### 9.5 新增验收项：八星互动盘（2026-06-23）

1. 中心命宫数与外围八方节点是否渲染正确，点击后信息面板是否同步更新。
2. 用户是否能从互动盘看懂“命宫数 + 方位落数 = 对应八星”的关系。
3. “先天八卦数 / 落盘数 / 组合数”的说明是否足够直观，不再只停留在文字卡片层。
4. 八星精简卡片在 390px 宽度下是否仍然规整、可读、无明显挤压。

### 9.6 新增执行进度：首页与返回入口 UI 重构（2026-06-23）

- `已完成`
  - 全局功能页返回栏已升级为粘性顶栏，左上返回按钮改为大触达胶囊样式。
  - 首页已从“功能列表”改为“主路径入口页”，补上路径提示、主入口分层和次级探索区。
  - 首页设置入口已改为带文字按钮，不再使用小齿轮式弱入口。
  - 主入口与快捷入口已补用途说明，降低新用户首次理解成本。

- `本轮验收重点`
  1. 首页首屏是否能一眼看出“先填生辰，再看档案，再学习或问事”的主路径。
  2. 返回按钮在长页面滚动后是否持续可见、可点、无遮挡。
  3. 首页主入口卡片与次级快捷入口在 375px 宽度下是否无横向滚动、无压缩错位。

- `下一步续做`
  - 继续收口命宫页和结果页的按钮、分区和弹层样式，使它们与首页入口语言统一。
  - 进入浏览器实测，确认这次首页改动不是“代码变了”，而是渲染后的感知确实变强。

### 9.7 新增执行进度：后天八卦盘与建档后引导（2026-06-23）

- `已完成`
  - 八卦互动盘已改为以后天八卦数为主口径展示八方节点与信息面板。
  - 修正了外围方位节点的定位问题，盘面不再只剩中心圆与下方信息卡。
  - 生辰信息提交后，命宫页已新增“建档完成 -> 回主路径首页”的明确承接动作。

- `本轮验收重点`
  1. 八个方位节点是否都出现在盘面外围，并能被点击或点按高亮。
  2. 点击方位后，标题、组合数、五行/星性、说明文案是否同步更新。
  3. 首次建档后，用户是否能明显知道下一步应该回首页继续主路径，而不是停在档案页里迷路。

- `下一步续做`
  - 继续把命宫页、结果页和首页之间的路径提示语言做统一。
  - 如果这版后天八卦盘验证通过，再继续补“看数字怎么用”的展开式说明，而不是只用 toast。

### 9.8 新增执行进度：八宅星位校正与盘面去卡片化（2026-06-23）

- `已完成`
  - `生气 / 天医` 映射已校正，`68` 现正确为 `天医`。
  - 互动盘已旋转为 `离上坎下` 的后天八卦朝向。
  - 八方节点已从卡片式外框改为八卦符号型节点，仅保留颜色与高亮态。
  - 建档完成引导卡文案已做最小化压缩。

- `本轮验收重点`
  1. `乾6 -> 艮8` 是否在 UI 上显示为 `天医`。
  2. 顶部节点是否变成 `离`，底部节点是否变成 `坎`。
  3. 去卡片化后，八方节点是否仍然容易点、不误触、信息切换清楚。
