# 阶段进度记录 2026-06-30

## 当日补充更新（山门式结构实现）

### 已完成

1. 首页已按 A「山门式三路径」进入主应用实现
   - 保留启动页与建档路径
   - 首页强化「今日主入口」
   - 三条主路明确为「每日一卦 / 看自己 / 问一事」
   - 图鉴、历史、数字起卦、重新排盘继续作为下沉工具入口
   - 移动端新增首页底部 4 项导航：今日、自己、问事、更多

2. AI 解读已改为全局弹窗反馈
   - 点击 AI 解读后立即弹出生成中状态
   - 生成完成后在弹窗中展示「卦里怎么看 / 放到主题里 / 今天先怎么做 / 边界提醒」
   - 生成失败时在弹窗中显示友好错误说明
   - 结果页内原有结构化内容仍保留，避免弹窗关闭后找不到内容

3. P1/P2 视觉层级已做第一轮统一
   - 每日一卦页、问一事结果页、命宫档案页统一为轻量水墨、石青主色、朱砂操作点
   - 关键面板改为更克制的宣纸/水墨质感
   - 移动端首页压缩首屏高度，隐藏次要回流提示，减少视觉疲劳

### 已验证

- 本地 `http://127.0.0.1:8080/` 可打开
- 默认入口仍为 `splash-screen`
- 点击开始后可进入 `home-screen`
- 首页底部导航存在且仅在首页显示
- AI 弹窗 DOM 与 JS 状态函数已接入
- 390px 手机宽度无横向溢出
- 控制台未发现 error/warning

### 待用户体验验收

1. 山门式首页是否比旧版更清楚、更高级
2. 首页首屏是否仍然偏长或信息偏多
3. AI 真实生成后弹窗阅读体验是否足够自然
4. 每日一卦、问一事、命宫档案三页风格是否统一
5. 文案内容是否还需要继续减少重复和模板感

## 当日补充更新（结构标准重建）

### 已完成

1. 已回退被否定的首页卷轴式 UI 重构
   - 恢复 `splash-screen` 作为默认首屏
   - 移除 `home-screen-rebuilt` 相关 CSS/JS
   - 恢复新手引导弹窗逻辑
   - 保留此前已通过的功能与页面收口，不做整文件回滚

2. 已建立新的 UI 开发前置标准
   - 新增 [docs/UI_STRUCTURE_STANDARD_V1.md](D:\vscode\Microsoft VS Code\project\Cculture\docs\UI_STRUCTURE_STANDARD_V1.md)
   - 明确首页、每日一卦、看自己、问一事、图鉴/历史/工具的层级边界
   - 明确后续 UI 先做结构确认，再进入主应用代码修改

3. 已生成三套结构原型
   - 新增 [prototypes/ui-structure-options-v1.html](D:\vscode\Microsoft VS Code\project\Cculture\prototypes\ui-structure-options-v1.html)
   - A：山门式三路径，推荐作为下一轮首页结构
   - B：今日轴心，适合强化复访
   - C：问答式入口，适合作为首次引导

### 当前建议

下一步先确认采用哪套首页结构。若无新决策，默认采用 A「山门式三路径」，原因是它最适合当前单文件 SPA、手机用户和现有三条主路径，不会继续堆大卡片。

## 当日补充更新（第二轮）

### 已新增完成

1. 开发工具与仓库基线恢复
   - 本地 `git`、`python`、`node`、`npm` 已确认可用
   - 当前工作目录已重新接回远端仓库 `sskks/guanxin`
   - 已验证 `git fetch origin --prune` 与 `git push --dry-run origin main`
   - 已补充工具基线文档：
     - [docs/DEV_TOOL_BASELINE.md](D:\vscode\Microsoft VS Code\project\Cculture\docs\DEV_TOOL_BASELINE.md)

2. 首页主路径继续收口
   - 已将“回看 / 练习 / 今日节律”收进折叠区，降低首页首屏噪音
   - 已将首页主路径改成更明确的“三条主路”
   - 已把图鉴、历史、工具入口继续维持在下沉区
   - 已用移动端宽度验证首页层级更容易分清

3. 命宫档案页首屏减重
   - 已缩短命宫首屏导语与行动提示长度
   - 已把底色层首屏信息从 3 张以上重复卡压缩为 2 个重点观察点
   - 已保留“今天先这样用”与四层入口，但减轻了首屏重复表述
   - 已用移动端宽度验证命宫页结构变化真实生效

### 当前真实状态

- 项目可继续开发
- 本地服务可运行
- 仓库已恢复版本控制能力
- 当前 P0 工作重点已从“环境恢复”切换到“页面层级与移动端体验收口”

### 下一步建议

1. 把“今日节律建议”逐步并入“每日一卦”页面层级，不长期停留在首页
2. 继续压缩命宫页首屏文字，把“今日先做 / 今日先不要 / 晚上回看”改成更强视觉分组
3. 继续处理问一事结果页首屏去重与 AI 追问区显隐节奏

## 阶段名称

仓库恢复后基线校准 + 标准档补齐 + 第一轮页面层级收口

## 本阶段目标

1. 从线上仓库恢复本地可运行基线
2. 补齐当前缺失的标准文档
3. 对首页、命宫档案页、问一事结果页做第一轮结构收口
4. 做本地可运行验证

## 本阶段已完成

### 1. 本地项目恢复

- 已从 GitHub 仓库 `sskks/guanxin` 主分支恢复当前项目基线
- 已确认当前真实运行入口仍为根目录 `index.html`
- 已恢复 `server.py`、`docs/`、`README.md`、`PRODUCT.md`、`DESIGN.md`

### 2. 标准文档补齐

已新增：

- [docs/PAGE_SPEC_V1.md](D:\vscode\Microsoft VS Code\project\Cculture\docs\PAGE_SPEC_V1.md)
- [docs/SKILL_ORCHESTRATION_AND_GITHUB_ADOPTION.md](D:\vscode\Microsoft VS Code\project\Cculture\docs\SKILL_ORCHESTRATION_AND_GITHUB_ADOPTION.md)

这两份文档明确了：

- 当前主路径链路
- 页面层级
- 首页 / 看自己 / 问一事的职责边界
- 本轮只做 P0 收口，不横向扩功能
- 各 skill 的职责分工与执行顺序

### 3. 第一轮页面层级收口

已修改文件：

- [index.html](D:\vscode\Microsoft VS Code\project\Cculture\index.html)

已完成的页面调整：

- 首页首屏文案收口：
  - 更强调“今天先从哪里开始”
  - 弱化说明板感
  - 强化主路径与次路径区分
- 首页主路径入口文案收口：
  - `看自己`
  - `每日一卦`
  - `问一事`
  三者现在更像模块入口，而不是重复解释卡
- 问一事结果页收口：
  - 将“继续往下看什么”改成更明确的深读提示
  - 强化“首屏够用，想学深再展开”的层级
- 命宫档案页四层切换收口：
  - 四层按钮补上更明确的层级提示
  - 从“说明区”更接近“导航区”

### 4. 样式层收口

已压缩以下区域的文字密度和卡片密度：

- 首页首屏提示条
- 首页返回提示条
- 首页次级入口说明
- 问一事结果页深读提示卡
- 命宫四层切换导航

## 本阶段验证结果

### 本地验证通过

- `server.py` 语法检查通过
- 本地服务已验证可返回 `HTTP 200`
- 本轮新增文档已成功写入本地工作区

### 本阶段验证命令结果

- `python -m py_compile .\\server.py`：通过
- `http://127.0.0.1:8080/`：返回 `200`

## 当前遗留问题

### 1. 仓库同步阻塞

当前本地目录存在以下问题：

- `.git` 目录不存在
- `git` 命令不可用

这意味着：

- 当前目录不是一个完整 Git 工作树
- 本阶段虽然已经完成本地文档和页面更新，但暂时无法直接提交并推送到仓库

### 2. 旧状态文档编码混乱

以下旧文档目前仍可读，但编码状态不稳定，不适合直接大范围重写：

- [docs/PROJECT_STATE.md](D:\vscode\Microsoft VS Code\project\Cculture\docs\PROJECT_STATE.md)
- [docs/DEVELOPMENT_PLAN.md](D:\vscode\Microsoft VS Code\project\Cculture\docs\DEVELOPMENT_PLAN.md)

因此本阶段采用了更安全的方案：

- 不重写旧文档主体
- 先新增 UTF-8 的阶段进度文档，保证记录不断档

## 下一阶段计划

### P0.1 文档与仓库环境修复

1. 恢复或重新接入 Git 工作树
2. 恢复仓库推送能力
3. 将本阶段成果正式同步到仓库
4. 再安全处理旧状态文档编码

### P0.2 页面继续收口

1. 首页首屏继续减噪
2. 问一事结果页继续压缩首屏重复信息
3. AI 追问区做移动端排版收口
4. 命宫档案页继续压缩底色层文字密度

## 阶段结论

本阶段已经完成“恢复基线”和“重新把开发拉回标准档约束”这两件关键事。

当前状态是：

- 可以继续开发
- 可以本地运行
- 标准文档已补齐
- 仓库同步暂时被 Git 环境缺失阻塞

---

## 当日补充更新（山门式 P0/P1/P2 一次性交付）

> 更新时间：2026-06-30

### P0 已完成：山门式首页与主路径层级

- 首页改为“今日主门 + 三扇门 + 下沉更多”的结构。
- 顶部只承担品牌与今日入口，不再铺长解释。
- 三扇门明确为：每日一卦、命宫档案、以事问卦。
- 练习、回看、节律收进折叠区，减少首屏噪音。
- 图鉴、历史、数字起卦、重新排盘继续下沉到“更多”。

### P1 已完成：核心页面体验收口

- 每日一卦页保留左上返回，默认先显示今日落点、先做什么、先别急什么。
- 每日一卦深读区改成学习入口，不默认把卦辞和长解释铺满。
- 问事结果页默认只露出本卦判断、动作、边界和深读入口。
- AI 追问区默认只显示“先看 AI 解读”，细分主题追问在 AI 生成后再展开。
- 命宫档案文案调整为“长期底色/观察点”，避免把固定命宫包装成每日变化。
- 命宫四层导航压缩为：底色、现实、八星、原文。

### P2 已完成：视觉与交互细节

- 首页补充山门式视觉骨架：轻水墨背景、门形路径卡、石青主色和宣纸质感。
- 底部导航保留 4 项：今日、自己、问事、更多。
- AI 弹窗升级为全局弹窗：加载、成功、失败都在弹窗中反馈。
- AI 生成完成后内容保留在弹窗，同时同步到结果页。
- 用户若生成期间切到别处，点击“继续追问此卦”会回到问事结果页输入区。
- 摇签交互改为“轻点或长按均可出签”，保留仪式感但避免用户误以为卡住。

### 已验证

- `python -m py_compile .\server.py`：通过。
- `http://127.0.0.1:8080/`：返回 `200`。
- 浏览器渲染验证：首页可进入，底部导航可见。
- 每日一卦页：左上返回存在，无横向溢出。
- 问事流程：点击问事、填写问题、摇签、翻转卡、查看完整解读可进入结果页。
- AI 弹窗：点击后立即出现加载态；生成完成后弹窗显示结构化结果；控制台无 error/warn。

### 未纳入本次

- 未做工程拆分，仍保持单文件 `index.html`。
- 未新增后端能力和商业化权限体系。
- 未继续扩展微社交、分享卡片 2.0。
- 未系统重写六十四卦完整文案库，本次只收口页面结构与交互。
