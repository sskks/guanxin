# packages/design-system

设计系统包，承接 UI 升级和后续前端拆分。

目标：

- 统一颜色、字体、间距、圆角、阴影、动效
- 提供可复用组件样式
- 保证“情绪安顿 + 传统文化 + 行动引导”的产品气质一致

计划文件：

- `tokens.css`：颜色、字体、间距、层级、动效 token
- `components.css`：按钮、顶部栏、行动卡、下一步面板、弹窗、Toast
- `patterns.md`：页面模式与组件使用规则

当前先以文档形式固化，后续从 `index.html` 逐步抽取。
